# v0.12.31 发布前报告

## 已实现

- `MemoryGovernanceKernel`：Candidate/Review/Ledger、scope、默认 TTL、冲突仲裁、过期扫描、会话摘要 digest。
- `WorkflowDagKernel`：DAG/JSON Schema 形状校验、循环与副作用校验、受控 JSON 投影、step checkpoint、resume drift、cancel/replan、Evaluation/Canary 生命周期门禁。
- `TaskStateKernel`：append-only `task_state_event`、revision projection、Evidence/actor/reason、并发冲突拒绝与 replay。
- 旧库迁移：仅通过 `scripts/import-legacy-knowledge.ts` 一次性离线读取 `data/pages/**`，confirmed 筛选、摘要/digest、candidate 导入、去重、Evidence 绑定、审核发布、撤回/tombstone、source diff 与失败报告；运行时 MCP/Host 不再扫描旧目录。
- Studio API、MCP surface、Internal Host 会话持久化均走受控 Kernel；旧 Memory 表增加敏感内容拒绝，兼容投影不作为新 Studio 写入目标。
- 包、插件清单、README 和适配器元数据统一为 `0.12.31`。

## 验证证据

- `node node_modules/typescript/bin/tsc --noEmit`：通过。
- `node --test --test-isolation=none tests/v01231-agent-foundation.test.ts tests/legacy-knowledge-migration.test.ts`：6/6 通过。
- `node --test --test-isolation=none tests/*.test.ts`（loopback 允许）：653/653 通过（包含 v0.12.31 覆盖补测与 Studio 兼容门面测试）。
- 离线导入 dry-run：53 个候选页、6 个排除项；正式导入：53 个 candidate、1 个 duplicate、0 个失败。
- `node --experimental-strip-types scripts/check-version.ts`：通过。
- `git diff --check`：通过。

## 阻塞与未验证项

- Node 23 内置 test runner 不提供 statements 覆盖率指标；本次 `pnpm run test:ts` 的全量结果为 lines 100%、functions 100%、branches 96.68%。分支覆盖仍为信息项，未使用 coverage ignore 伪造结果。若发布门禁必须四项均 100%，需接入支持 statements/branch 精确报告的 c8/nyc 或 Istanbul 工具后再加门禁。
- 生产远程 Adapter、OIDC、Secret Broker、对象存储和分布式 Lease 仍只有契约/本地 conformance；本地构建、插件 smoke 和 loopback 测试均已完成。
