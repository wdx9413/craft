# Branch coverage gap report

- Generated from: `/Users/didi/Documents/work/craft/coverage/coverage-final.json`
- Source scope: runtime `src/**/*.ts` (CLI and declaration-only `.d.ts` files are not runtime logic)
- Covered branches: 18468/18923 (97.60%)
- Missing branches: 455

| File | Branch | Source location | Hits | Expression |
| --- | --- | ---: | ---: | --- |
| `src/a2a-transport.ts` | `35.0` | 20:393 | 0 | `const response = await fetchImpl(`${endpoint.replace(/\/$/u, "")}/tasks/${encodeURIComponent(taskId)}:cancel`, { method: "POST", headers: { "content-type": "application/json", accept: "application/json" }, body: JSON.stringify({ task_id: taskId, reason_digest: digest(args.reason ?? "user_requested"), execution_authority: false }) }); if (response.status < 200 \|\| response.status >= 300) throw new Error(`A2A cancel HTTP ${response.status}`);` |
| `src/a2a-v1-adapter.ts` | `27.0` | 31:53 | 0 | `const observationId = String(args.observation_id ?? `a2a_v1_card_${digest(identity).slice(-24)}`); const existing = this.store.find("a2a_v1_card_observation", observationId); const observationDigest = digest(identity);` |
| `src/a2a-v1-adapter.ts` | `29.0` | 32:75 | 0 | `if (existing) { if (existing.observation_digest !== observationDigest) throw new Error("A2A v1 card observation idempotency conflict"); return { observation: existing, idempotent: true }; }` |
| `src/a2a-v1-adapter.ts` | `36.0` | 41:39 | 0 | `const taskId = String(args.task_id ?? `a2a_v1_task_${randomUUID().replaceAll("-", "")}`); const existing = this.store.find("a2a_v1_task", taskId); const taskDigest = digest(identity);` |
| `src/a2a-v1-adapter.ts` | `38.0` | 42:61 | 0 | `if (existing) { if (existing.task_digest !== taskDigest) throw new Error("A2A v1 task idempotency conflict"); return { task: existing, idempotent: true }; }` |
| `src/a2a-v1-adapter.ts` | `40.0` | 44:269 | 0 | `const response = await fetchImpl(String(card.card_url), { method: "POST", headers: { "content-type": "application/json", accept: "application/json", "idempotency-key": requestId }, body: JSON.stringify(body) }); if (response.status < 200 \|\| response.status >= 300) throw new Error(`A2A v1 message/send HTTP ${response.status}`);` |
| `src/a2a-v1-adapter.ts` | `50.0` | 55:87 | 0 | `const result = object(await response.json(), "A2A v1 response"); if (result.error) throw new Error("A2A v1 remote returned an error"); const remote = object(result.result, "A2A v1 result"); const status = taskState(remote);` |
| `src/a2a-v1-adapter.ts` | `52.0` | 56:121 | 0 | `if (!new Set(["submitted", "working", "input-required", "completed", "failed", "canceled", "rejected"]).has(status)) throw new Error("A2A v1 task state is unsupported");` |
| `src/a2a-v1-adapter.ts` | `57.0` | 65:87 | 0 | `const result = object(await response.json(), "A2A v1 response"); if (result.error) throw new Error("A2A v1 remote returned an error"); const remote = object(result.result, "A2A v1 result"); const status = taskState(remote);` |
| `src/application/craft-service.ts` | `125.0` | 249:192 | 0 | `const evidence = args.evidence_ids === undefined ? this.evidenceRecord({ source_type: "human", confidence: "bounded", claim: "Studio-authored candidate.", locator: "studio://knowledge" }) : null;` |
| `src/application/craft-service.ts` | `129.0` | 254:44 | 0 | `const nodes = Array.isArray(args.nodes) ? args.nodes : steps.map((step, index) => ({ id: String(step.id ?? `step-${index + 1}`), type: String(step.type ?? "action"), side_effect: String(step.side_effect ?? "read_only"), ...(step.action === undefined ? {} : { action: step.action }), depends_on: Array.isArray(step.depends_on) ? step.depends_on : [] }));` |
| `src/application/craft-service.ts` | `133.0` | 254:108 | 0 | `const nodes = Array.isArray(args.nodes) ? args.nodes : steps.map((step, index) => ({ id: String(step.id ?? `step-${index + 1}`), type: String(step.type ?? "action"), side_effect: String(step.side_effect ?? "read_only"), ...(step.action === undefined ? {} : { action: step.action }), depends_on: Array.isArray(step.depends_on) ? step.depends_on : [] }));` |
| `src/application/craft-service.ts` | `138.0` | 254:330 | 0 | `const nodes = Array.isArray(args.nodes) ? args.nodes : steps.map((step, index) => ({ id: String(step.id ?? `step-${index + 1}`), type: String(step.type ?? "action"), side_effect: String(step.side_effect ?? "read_only"), ...(step.action === undefined ? {} : { action: step.action }), depends_on: Array.isArray(step.depends_on) ? step.depends_on : [] }));` |
| `src/application/craft-service.ts` | `140.0` | 258:48 | 0 | `const now = new Date(args.now === undefined ? Date.now() : text(args.now, "now")); if (Number.isNaN(now.valueOf())) throw new Error("now must be an ISO timestamp"); const expired: JsonObject[] = [];` |
| `src/application/craft-service.ts` | `141.0` | 258:120 | 0 | `const now = new Date(args.now === undefined ? Date.now() : text(args.now, "now")); if (Number.isNaN(now.valueOf())) throw new Error("now must be an ISO timestamp"); const expired: JsonObject[] = [];` |
| `src/application/craft-service.ts` | `145.0` | 264:115 | 0 | `if (decision === "reviewed") { const ids = Array.isArray(claim.evidence_ids) ? claim.evidence_ids as unknown[] : []; if (!ids.some((e) => ["bounded", "confirmed"].includes(String(this.store.get("evidence", String(e)).confidence)))) throw new Error("Conflict resolution requires bounded or confirmed Evidence"); }` |
| `src/application/craft-service.ts` | `146.0` | 264:236 | 0 | `if (decision === "reviewed") { const ids = Array.isArray(claim.evidence_ids) ? claim.evidence_ids as unknown[] : []; if (!ids.some((e) => ["bounded", "confirmed"].includes(String(this.store.get("evidence", String(e)).confidence)))) throw new Error("Conflict resolution requires bounded or confirmed Evidence"); }` |
| `src/application/craft-service.ts` | `158.0` | 288:334 | 0 | `const prepared = this.actionGatewayPrepare({ action_id: `internal-write-${valueDigest(input).slice(7, 23)}`, task_id: text(input.task_id, "task_id"), workspace: text(input.workspace, "workspace"), operation: "workspace_write", effect: "local_write", input_digest: valueDigest(input), ...(input.approval_ref === undefined ? {} : { approval_ref: input.approval_ref }) });` |
| `src/application/craft-service.ts` | `660.0` | 1055:97 | 0 | `const unsafe = (workflow.steps as JsonObject[] ?? []).find((step) => String(step.side_effect ?? "read_only") !== "read_only");` |
| `src/application/craft-service.ts` | `954.0` | 1432:54 | 0 | ``Permission mode: ${String(task.permission_mode ?? "human_approval")}.`,` |
| `src/application/craft-service.ts` | `956.0` | 1448:140 | 0 | `this.store.appendEvent(`task:${taskId}`, "task.message.failed", { model_id: modelId, error_class: error instanceof Error ? error.name : "UnknownError" });` |
| `src/application/craft-service.ts` | `1322.0` | 1969:86 | 0 | `const candidates = this.store.list("memory_candidate", limit, (item) => !taskId \|\| item.task_id === taskId);` |
| `src/application/craft-service.ts` | `1336.0` | 2007:33 | 0 | `if (content.length > 48_000) throw new Error("Skill content must not exceed 48,000 characters");` |
| `src/application/craft-service.ts` | `1339.0` | 2012:29 | 0 | `return { skill: previous ? this.store.save("studio_skill", skillId, { ...payload, previous_version: previous.version }) : this.store.create("studio_skill", skillId, payload) };` |
| `src/application/craft-service.ts` | `1341.0` | 2015:62 | 0 | `const memoryId = args.memory_id === undefined ? undefined : text(args.memory_id, "memory_id");` |
| `src/application/craft-service.ts` | `1342.0` | 2016:51 | 0 | `const previous = memoryId === undefined ? null : this.store.get("memory_item", memoryId);` |
| `src/application/craft-service.ts` | `1343.0` | 2017:49 | 0 | `return this.memoryRemember({ kind: args.kind ?? previous?.kind ?? "fact", scope: args.scope ?? previous?.scope ?? "user",` |
| `src/application/craft-service.ts` | `1344.0` | 2017:67 | 0 | `return this.memoryRemember({ kind: args.kind ?? previous?.kind ?? "fact", scope: args.scope ?? previous?.scope ?? "user",` |
| `src/application/craft-service.ts` | `1345.0` | 2017:96 | 0 | `return this.memoryRemember({ kind: args.kind ?? previous?.kind ?? "fact", scope: args.scope ?? previous?.scope ?? "user",` |
| `src/application/craft-service.ts` | `1346.0` | 2017:115 | 0 | `return this.memoryRemember({ kind: args.kind ?? previous?.kind ?? "fact", scope: args.scope ?? previous?.scope ?? "user",` |
| `src/application/craft-service.ts` | `1347.0` | 2018:85 | 0 | `content: args.content, source: "studio_user", task_id: args.task_id ?? previous?.task_id ?? undefined,` |
| `src/application/craft-service.ts` | `1348.0` | 2019:49 | 0 | `workspace_id: args.workspace_id ?? previous?.workspace_id ?? undefined, applies_to: args.applies_to ?? previous?.applies_to ?? [],` |
| `src/application/craft-service.ts` | `1349.0` | 2019:117 | 0 | `workspace_id: args.workspace_id ?? previous?.workspace_id ?? undefined, applies_to: args.applies_to ?? previous?.applies_to ?? [],` |
| `src/application/craft-service.ts` | `1350.0` | 2020:37 | 0 | `evidence_ids: [], ...(previous ? { supersedes_id: previous.id } : {}) });` |
| `src/application/craft-service.ts` | `1354.0` | 2028:230 | 0 | `studioWorkflowCompatSave(args: JsonObject): JsonObject { return this.workflowSave({ workflow_id: args.workflow_id, name: text(args.name, "name"), description: args.description ?? "", inputs: args.inputs ?? [], steps: args.steps ?? [] }); }` |
| `src/application/craft-service.ts` | `1982.0` | 2309:94 | 0 | `const evidenceIds = Array.isArray(claim.evidence_ids) ? claim.evidence_ids as unknown[] : [];` |
| `src/application/craft-service.ts` | `2308.0` | 2584:140 | 0 | `const workspaceId = args.workspace_id === undefined ? `workspace_loop_${valueDigest({ root, include_paths: includePaths }).slice(-16)}` : text(args.workspace_id, "workspace_id"); const existing = this.store.find("workspace", workspaceId);` |
| `src/application/craft-service.ts` | `2310.0` | 2585:123 | 0 | `const workspace = existing ?? (this.workspaceOpen({ workspace_id: workspaceId, name: args.workspace_name ?? args.title ?? "Craft Task Workspace", root_path: root, include_paths: includePaths }).workspace as JsonObject);` |
| `src/application/craft-service.ts` | `2311.0` | 2586:108 | 0 | `if (workspace.root_path !== root \|\| valueDigest(workspace.include_paths) !== valueDigest(includePaths)) throw new Error("Task Workspace id is already bound to another root or observation scope");` |
| `src/application/craft-service.ts` | `2335.0` | 2624:98 | 0 | `const prompt = text(args.prompt, "prompt"); if (valueDigest(prompt) !== launch.prompt_digest) throw new Error("Execution Fabric prompt does not match the prepared digest");` |
| `src/application/craft-service.ts` | `2518.0` | 2859:13 | 0 | `if (!id) throw new Error("model id is required");` |
| `src/application/use-cases/adapter-runtime.ts` | `10.0` | 47:187 | 0 | `serviceClass.prototype.adapterInstall = async function (args) { return new V01226Runtime(this.store).adapterInstall(String(args.manifest_path), args.integrity === undefined ? undefined : String(args.integrity)); };` |
| `src/application/use-cases/adapter-runtime.ts` | `18.0` | 56:83 | 0 | `candidates: Array.isArray(args.candidates) ? args.candidates as JsonObject[] : [],` |
| `src/application/use-cases/adapter-runtime.ts` | `19.0` | 57:45 | 0 | `required: Array.isArray(args.required) ? args.required as string[] : [],` |
| `src/application/use-cases/adapter-runtime.ts` | `20.0` | 58:64 | 0 | `token_budget: args.token_budget === undefined ? undefined : Number(args.token_budget),` |
| `src/application/use-cases/adapter-runtime.ts` | `26.0` | 64:157 | 0 | `serviceClass.prototype.durableRunRecover = function (args = {}) { return new V01226Runtime(this.store).durableRecover(args.owner === undefined ? undefined : String(args.owner)); };` |
| `src/application/use-cases/adapter-runtime.ts` | `28.0` | 65:247 | 0 | `serviceClass.prototype.trustCurveRecord = function (args) { return new V01226Runtime(this.store).trustRecord({ scope: String(args.scope), passed: Number(args.passed), failed: Number(args.failed), evidence_refs: Array.isArray(args.evidence_refs) ? args.evidence_refs as string[] : [] }); };` |
| `src/application/use-cases/adapter-runtime.ts` | `30.0` | 66:189 | 0 | `serviceClass.prototype.modelRouteV01226 = function (args) { return new V01226Runtime(this.store).modelRoute({ candidates: Array.isArray(args.candidates) ? args.candidates as JsonObject[] : [], objective: args.objective as "quality" \| "cost" \| "latency" \| undefined, budget: args.budget === undefined ? undefined : Number(args.budget) }); };` |
| `src/application/use-cases/adapter-runtime.ts` | `31.0` | 66:314 | 0 | `serviceClass.prototype.modelRouteV01226 = function (args) { return new V01226Runtime(this.store).modelRoute({ candidates: Array.isArray(args.candidates) ? args.candidates as JsonObject[] : [], objective: args.objective as "quality" \| "cost" \| "latency" \| undefined, budget: args.budget === undefined ? undefined : Number(args.budget) }); };` |
| `src/application/use-cases/adapter-runtime.ts` | `33.0` | 67:186 | 0 | `serviceClass.prototype.deliveryGateV01226 = function (args) { return new V01226Runtime(this.store).deliveryGate({ artifacts: Array.isArray(args.artifacts) ? args.artifacts as string[] : [], evidence: Array.isArray(args.evidence) ? args.evidence as string[] : [], required_artifacts: Array.isArray(args.required_artifacts) ? args.required_artifacts as string[] : [], required_evidence: Array.isArray(args.required_evidence) ? args.required_evidence as string[] : [] }); };` |
| `src/application/use-cases/adapter-runtime.ts` | `34.0` | 67:259 | 0 | `serviceClass.prototype.deliveryGateV01226 = function (args) { return new V01226Runtime(this.store).deliveryGate({ artifacts: Array.isArray(args.artifacts) ? args.artifacts as string[] : [], evidence: Array.isArray(args.evidence) ? args.evidence as string[] : [], required_artifacts: Array.isArray(args.required_artifacts) ? args.required_artifacts as string[] : [], required_evidence: Array.isArray(args.required_evidence) ? args.required_evidence as string[] : [] }); };` |
| `src/application/use-cases/adapter-runtime.ts` | `35.0` | 67:324 | 0 | `serviceClass.prototype.deliveryGateV01226 = function (args) { return new V01226Runtime(this.store).deliveryGate({ artifacts: Array.isArray(args.artifacts) ? args.artifacts as string[] : [], evidence: Array.isArray(args.evidence) ? args.evidence as string[] : [], required_artifacts: Array.isArray(args.required_artifacts) ? args.required_artifacts as string[] : [], required_evidence: Array.isArray(args.required_evidence) ? args.required_evidence as string[] : [] }); };` |
| `src/application/use-cases/adapter-runtime.ts` | `36.0` | 67:425 | 0 | `serviceClass.prototype.deliveryGateV01226 = function (args) { return new V01226Runtime(this.store).deliveryGate({ artifacts: Array.isArray(args.artifacts) ? args.artifacts as string[] : [], evidence: Array.isArray(args.evidence) ? args.evidence as string[] : [], required_artifacts: Array.isArray(args.required_artifacts) ? args.required_artifacts as string[] : [], required_evidence: Array.isArray(args.required_evidence) ? args.required_evidence as string[] : [] }); };` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `2.0` | 4:126 | 0 | `function candidateScope(candidate: JsonObject): string { return `legacy:${String(candidate.scope)}:${String(candidate.project ?? "shared")}`; }` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `4.0` | 5:153 | 0 | `function memoryKind(candidate: JsonObject): string { return ["workflow", "technical_decision"].includes(String(candidate.knowledge_type)) ? "procedural" : "working"; }` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `7.0` | 9:31 | 0 | `if (args.offline !== true) throw new Error("Legacy knowledge discovery is offline-only; use scripts/import-legacy-knowledge.ts");` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `9.0` | 13:31 | 0 | `if (args.offline !== true) throw new Error("Legacy knowledge import is offline-only; use scripts/import-legacy-knowledge.ts");` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `11.0` | 20:365 | 0 | `this.knowledgeClaimSave({ claim_id: claimId, kind: ({ domain_rule: "fact", technical_decision: "decision", workflow: "rule", troubleshooting: "failure_mode", stable_project_fact: "fact" } as Record<string, string>)[entry.knowledge_type], content: `${entry.title}\n\n${entry.summary}`, evidence_ids: [evidenceId], scope: `legacy:${entry.scope}:${entry.project ?? "shared"}`, tags: entry.tags });` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `14.0` | 26:94 | 0 | `const candidate = prepared.candidate as JsonObject; const reviewer = String(args.reviewer ?? "").trim(); if (!reviewer) throw new Error("reviewer must not be empty");` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `15.0` | 26:124 | 0 | `const candidate = prepared.candidate as JsonObject; const reviewer = String(args.reviewer ?? "").trim(); if (!reviewer) throw new Error("reviewer must not be empty");` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `17.0` | 32:6 | 0 | `} catch (error) { this.legacyKnowledgeMigration.recordFailure(String(candidate.migration_id), "publish", String(candidate.source_locator), error instanceof Error ? error.message : "publish_failed"); throw error; }` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `20.0` | 36:94 | 0 | `const candidate = prepared.candidate as JsonObject; const reviewer = String(args.reviewer ?? "").trim(); const reason = String(args.reason ?? "").trim(); if (!reviewer \|\| !reason) throw new Error("reviewer and reason must not be empty");` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `21.0` | 36:143 | 0 | `const candidate = prepared.candidate as JsonObject; const reviewer = String(args.reviewer ?? "").trim(); const reason = String(args.reason ?? "").trim(); if (!reviewer \|\| !reason) throw new Error("reviewer and reason must not be empty");` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `23.0` | 36:184 | 0 | `const candidate = prepared.candidate as JsonObject; const reviewer = String(args.reviewer ?? "").trim(); const reason = String(args.reason ?? "").trim(); if (!reviewer \|\| !reason) throw new Error("reviewer and reason must not be empty");` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `25.0` | 42:6 | 0 | `} catch (error) { this.legacyKnowledgeMigration.recordFailure(String(candidate.migration_id), "retract", String(candidate.source_locator), error instanceof Error ? error.message : "retract_failed"); throw error; }` |
| `src/capability-connector.ts` | `21.0` | 27:86 | 0 | `if (!result.length \|\| result.length > 20 \|\| new Set(result).size !== result.length) throw new Error("allowed_operations must contain between 1 and 20 unique entries");` |
| `src/capability-connector.ts` | `77.0` | 112:64 | 0 | `if (!["healthy", "degraded", "unhealthy"].includes(status)) throw new Error("Connector health status is unsupported");` |
| `src/capability-connector.ts` | `84.0` | 118:37 | 0 | `if (args.health_id !== undefined && text(args.health_id, "health_id") !== healthId) throw new Error("Connector health id is derived from connector_id");` |
| `src/capability-connector.ts` | `85.0` | 118:88 | 0 | `if (args.health_id !== undefined && text(args.health_id, "health_id") !== healthId) throw new Error("Connector health id is derived from connector_id");` |
| `src/capability-connector.ts` | `87.0` | 126:6 | 0 | `: this.store.create("capability_connector_health", healthId, healthPayload);` |
| `src/capability-connector.ts` | `136.0` | 206:86 | 0 | `connector_scope_digest: connector.scope_digest, connector_health_id: health?.id ?? null, connector_health_version: health?.version ?? null,` |
| `src/capability-connector.ts` | `138.0` | 206:137 | 0 | `connector_scope_digest: connector.scope_digest, connector_health_id: health?.id ?? null, connector_health_version: health?.version ?? null,` |
| `src/capability-connector.ts` | `146.0` | 218:66 | 0 | `if (connector.scope_digest !== ticket.connector_scope_digest) throw new Error("Connector scope changed after ticket issue");` |
| `src/home.ts` | `126.0` | 82:98 | 0 | `const knowledge = this.store.list("knowledge_claim", limit, (item) => item.task_id === taskId \|\| item.scope === `task:${taskId}`);` |
| `src/host-run.ts` | `43.0` | 26:1392 | 0 | `const completion = driver.execute({ ...args, dispatch_id: dispatchId, prompt }, { signal: controller.signal, observe }).then((result) => { const current = this.store.get("host_run", runId); const receipt = result.receipt as JsonObject; const status = current.cancel_requested \|\| receipt.cancelled ? "cancelled" : receipt.status === "completed" ? "completed" : "failed"; const saved = this.store.save("host_run", runId, { ...payload(current), status, receipt_id: receipt.id, finished_at: new Date().toISOString() }); this.store.appendEvent(`host-run:${runId}`, "host.finished", { status, receipt_id: receipt.id }); if (this.trace) { this.trace.append({ trace_id: traceId, event_kind: "host.finished", actor: "host-run", source: "host-run", trust: "observed", data: {}, output_refs: [String(receipt.id)], status, summary: "Host execution finished" }); this.trace.finalize({ trace_id: traceId, status: status === "completed" ? "completed" : status === "cancelled" ? "cancelled" : "failed", summary: "Host run terminal" }); } this.notifyTerminal(saved, receipt); }).catch((error) => { const current = this.store.get("host_run", runId); const saved = this.store.save("host_run", runId, { ...payload(current), status: current.cancel_requested ? "cancelled" : "failed", error_class: error instanceof Error ? error.name : "UnknownError", finished_at: new Date().toISOString() }); if (this.trace) { this.trace.append({ trace_id: traceId, event_kind: "host.failed", actor: "host-run", source: "host-run", trust: "observed", data: {}, error_class: error instanceof Error ? error.name : "UnknownError", status: "failed", summary: "Host execution failed" }); this.trace.finalize({ trace_id: traceId, status: "failed", summary: "Host execution failed" }); } this.notifyTerminal(saved, null); }).finally(() => { this.controllers.delete(runId); });` |
| `src/intent-compiler.ts` | `26.0` | 38:48 | 0 | `if (/(?:方法\|函数\|method\|function)/iu.test(goal)) return "methods";` |
| `src/intent-compiler.ts` | `28.0` | 39:33 | 0 | `if (/(?:行\|line)/iu.test(goal)) return "lines";` |
| `src/intent-compiler.ts` | `30.0` | 40:36 | 0 | `if (/(?:分支\|branch)/iu.test(goal)) return "branches";` |
| `src/intent-compiler.ts` | `51.0` | 62:34 | 0 | `if (/(?:测试\|test)/iu.test(goal)) return "test_verification";` |
| `src/intent-compiler.ts` | `72.0` | 86:110 | 0 | `const materials = args.materials === undefined ? [] : [...(Array.isArray(args.materials) ? args.materials : [args.materials]).map((value) => text(value, "materials"))];` |
| `src/intent-compiler.ts` | `75.0` | 87:92 | 0 | `const nonGoals = args.non_goals === undefined ? [] : [...(Array.isArray(args.non_goals) ? args.non_goals : [args.non_goals]).map((value) => text(value, "non_goals"))];` |
| `src/intent-compiler.ts` | `78.0` | 88:144 | 0 | `const allowedEffects = args.allowed_effects === undefined ? ["read_only"] : [...(Array.isArray(args.allowed_effects) ? args.allowed_effects : [args.allowed_effects]).map((value) => text(value, "allowed_effects"))];` |
| `src/intent-compiler.ts` | `88.0` | 94:63 | 0 | `baseline: args.baseline === undefined ? "git-merge-base" : text(args.baseline, "baseline"),` |
| `src/intent-compiler.ts` | `99.0` | 105:43 | 0 | `const intentId = String(args.intent_id ?? `intent_${randomUUID().replaceAll("-", "")}`);` |
| `src/intent-compiler.ts` | `116.0` | 123:82 | 0 | `id: "coverage", name: `${String(intent.scope) === "changed" ? "Incremental" : "Workspace"} ${String(metric)} coverage`, method: "program", evaluator: "coverage_report",` |
| `src/intent-compiler.ts` | `121.0` | 134:71 | 0 | `if (existing.definition_digest !== definition.definition_digest) throw new Error("Acceptance contract idempotency conflict");` |
| `src/internal-host-driver.ts` | `11.0` | 60:102 | 0 | `const raw = typeof message.content === "string" ? message.content : message.content === null ? "" : JSON.stringify(message.content);` |
| `src/internal-host-driver.ts` | `48.0` | 123:55 | 0 | `session_id: args.session_id === undefined ? null : text(args.session_id, "session_id"),` |
| `src/internal-host-driver.ts` | `50.0` | 124:63 | 0 | `context_digest: args.context_digest === undefined ? null : text(args.context_digest, "context_digest"),` |
| `src/internal-host-driver.ts` | `52.0` | 125:63 | 0 | `acceptance_ref: args.acceptance_ref === undefined ? null : text(args.acceptance_ref, "acceptance_ref") };` |
| `src/internal-host-driver.ts` | `128.0` | 159:181 | 0 | `? (session.messages as JsonObject[]).map((item) => ({ role: String(item.role) as ConversationMessage["role"], content: typeof item.content === "string" ? redact(item.content) : null }))` |
| `src/internal-host-driver.ts` | `131.0` | 173:178 | 0 | `const parsedCalls = (result.tool_calls ?? []).map((call) => ({ id: call.id, name: call.function.name, arguments: (() => { try { return JSON.parse(call.function.arguments \|\| "{}"); } catch { throw new Error("tool arguments must be valid JSON"); } })() }));` |
| `src/internal-host-driver.ts` | `132.0` | 173:190 | 0 | `const parsedCalls = (result.tool_calls ?? []).map((call) => ({ id: call.id, name: call.function.name, arguments: (() => { try { return JSON.parse(call.function.arguments \|\| "{}"); } catch { throw new Error("tool arguments must be valid JSON"); } })() }));` |
| `src/internal-host-driver.ts` | `91.0` | 177:37 | 0 | `finalMessage = result.text \|\| (parsedCalls.length ? `Tool call ${parsedCalls[0]!.name} not executed` : "");` |
| `src/legacy-knowledge-migration.ts` | `6.0` | 19:102 | 0 | `function text(value: unknown, name: string): string { if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`); return value.trim(); }` |
| `src/legacy-knowledge-migration.ts` | `13.0` | 24:79 | 0 | `const end = lines.slice(1).findIndex((line) => line === "---"); if (end < 0) throw new Error("frontmatter is not closed");` |
| `src/legacy-knowledge-migration.ts` | `34.0` | 45:174 | 0 | `for (const [path, entry] of after) if (before.get(path)?.page_digest !== entry.page_digest) changed.push({ rel_path: path, previous_digest: before.get(path)?.page_digest ?? null, current_digest: entry.page_digest, eligibility: entry.eligibility });` |
| `src/legacy-knowledge-migration.ts` | `45.0` | 55:53 | 0 | `if (relative(sourceRoot, root).startsWith("..")) throw new Error("data/pages escapes source_root");` |
| `src/legacy-knowledge-migration.ts` | `49.0` | 59:49 | 0 | `const migrationId = String(args.migration_id ?? `legacy_knowledge_migration_${digest({ sourceRoot, manifest }).slice(-24)}`);` |
| `src/legacy-knowledge-migration.ts` | `51.0` | 62:71 | 0 | `if (existing) { if (existing.identity_digest !== digest(identity)) throw new Error("Legacy knowledge discovery idempotency conflict"); return { migration: existing, idempotent: true }; }` |
| `src/legacy-knowledge-migration.ts` | `57.0` | 69:124 | 0 | `const selected = Array.isArray(args.candidate_ids) ? args.candidate_ids.map((item) => text(item, "candidate_ids item")) : [];` |
| `src/legacy-knowledge-migration.ts` | `64.0` | 75:139 | 0 | `if (entry.eligibility !== "eligible") { failures.push(this.failure(String(migration.id), "import", relPath, `excluded_${entry.reason ?? "policy"}`)); continue; }` |
| `src/legacy-knowledge-migration.ts` | `78.0` | 80:149 | 0 | `const duplicate = this.store.list("legacy_knowledge_migration_candidate", MAX_FILES, (item) => item.canonical_key === this.canonicalKey(entry) && item.status !== "retracted")[0];` |
| `src/legacy-knowledge-migration.ts` | `69.0` | 81:21 | 0 | `if (duplicate) { candidates.push(this.store.create("legacy_knowledge_migration_candidate", candidate_id, { migration_id: migration.id, source_id: sourceId, source_locator: entry.rel_path, source_digest: entry.page_digest, canonical_key: this.canonicalKey(entry), title: entry.title, summary: entry.summary, category: entry.category, knowledge_type: entry.knowledge_type, scope: entry.scope, project: entry.project, tags: entry.tags, status: "duplicate", duplicate_of: duplicate.id, evidence_id: null, claim_id: null, memory_id: null, wiki_page_id: null, import_authority: false })); continue; }` |
| `src/legacy-knowledge-migration.ts` | `71.0` | 85:8 | 0 | `} catch (error) { failures.push(this.failure(String(migration.id), "import", relPath, error instanceof Error ? error.message : "candidate_import_failed")); }` |
| `src/legacy-knowledge-migration.ts` | `82.0` | 93:42 | 0 | `if (candidate.status !== "candidate") throw new Error("Only a non-duplicate candidate can be published");` |
| `src/legacy-knowledge-migration.ts` | `88.0` | 106:42 | 0 | `if (candidate.status !== "published") throw new Error("Only a published candidate can be retracted");` |
| `src/legacy-knowledge-migration.ts` | `97.0` | 121:81 | 0 | `const target = resolve(directory, item.name); if (item.isSymbolicLink()) continue;` |
| `src/legacy-knowledge-migration.ts` | `100.0` | 122:159 | 0 | `if (item.isDirectory()) await walk(target); else if (item.isFile() && item.name.endsWith(".md")) { result.push(target); if (result.length > MAX_FILES) throw new Error("legacy knowledge file limit exceeded"); }` |
| `src/legacy-knowledge-migration.ts` | `108.0` | 132:82 | 0 | `const category = metadata.category ?? relative(pagesRoot, file).split("/")[0] ?? ""; const knowledge_type = metadata.knowledge_type ?? "";` |
| `src/legacy-knowledge-migration.ts` | `121.0` | 136:72 | 0 | `const summary = `来源摘要：${reuse}`; if (!safe(`${title}\n${summary}`)) return this.excluded(rel_path, page_digest, "sensitive_or_disallowed", title, category, knowledge_type);` |
| `src/legacy-knowledge-migration.ts` | `126.0` | 142:56 | 0 | `if (!relativePath \|\| relativePath.startsWith("..")) return "invalid_source_locator";` |
| `src/legacy-knowledge-migration.ts` | `127.0` | 144:100 | 0 | `const resolved = await realpath(target); if (relative(sourceRoot, resolved).startsWith("..")) return "source_path_escape";` |
| `src/legacy-knowledge-migration.ts` | `128.0` | 145:86 | 0 | `const info = await stat(resolved); if (!info.isFile() \|\| info.size > MAX_BYTES) return "source_unavailable";` |
| `src/legacy-knowledge-migration.ts` | `129.0` | 146:76 | 0 | `const content = await readFile(resolved, "utf8"); if (!safe(content)) return "source_sensitive_or_disallowed";` |
| `src/legacy-knowledge-migration.ts` | `132.0` | 148:6 | 0 | `} catch { return "source_unavailable"; }` |
| `src/legacy-knowledge-migration.ts` | `137.0` | 153:177 | 0 | `private failure(migration_id: string, stage: string, source_locator: string, code: string): JsonObject { return { migration_id, stage, source_locator, code: safe(code) ? code : "redacted_failure", failure_digest: digest(code) }; }` |
| `src/long-task-worker.ts` | `2.0` | 4:102 | 0 | `function text(value: unknown, name: string): string { if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`); return value.trim(); }` |
| `src/long-task-worker.ts` | `8.0` | 7:161 | 0 | `function now(value: unknown): string { const result = value === undefined ? new Date().toISOString() : text(value, "now"); if (Number.isNaN(Date.parse(result))) throw new Error("now must be an ISO timestamp"); return result; }` |
| `src/long-task-worker.ts` | `12.0` | 15:143 | 0 | `const session = this.store.get("work_session", text(args.session_id, "session_id")); const taskRun = args.task_run_id === undefined ? null : this.store.get("task_run", text(args.task_run_id, "task_run_id"));` |
| `src/long-task-worker.ts` | `13.0` | 16:16 | 0 | `if (taskRun && taskRun.contract_id && taskRun.launch_id && this.store.get("work_launch", String(taskRun.launch_id)).task_id !== session.task_id) throw new Error("Task Run does not belong to the session");` |
| `src/long-task-worker.ts` | `14.0` | 16:39 | 0 | `if (taskRun && taskRun.contract_id && taskRun.launch_id && this.store.get("work_launch", String(taskRun.launch_id)).task_id !== session.task_id) throw new Error("Task Run does not belong to the session");` |
| `src/long-task-worker.ts` | `15.0` | 16:60 | 0 | `if (taskRun && taskRun.contract_id && taskRun.launch_id && this.store.get("work_launch", String(taskRun.launch_id)).task_id !== session.task_id) throw new Error("Task Run does not belong to the session");` |
| `src/long-task-worker.ts` | `16.0` | 16:149 | 0 | `if (taskRun && taskRun.contract_id && taskRun.launch_id && this.store.get("work_launch", String(taskRun.launch_id)).task_id !== session.task_id) throw new Error("Task Run does not belong to the session");` |
| `src/long-task-worker.ts` | `19.0` | 17:356 | 0 | `const checkpointId = String(args.checkpoint_id ?? `long_task_${randomUUID().replaceAll("-", "")}`); const waitCondition = text(args.wait_condition, "wait_condition"); const resumeAction = text(args.resume_action ?? "revalidate_and_resume", "resume_action"); const state = { session_id: session.id, session_version: session.version, task_run_id: taskRun?.id ?? null, task_run_version: taskRun?.version ?? null, host_run_id: args.host_run_id ?? null, wait_condition: waitCondition, resume_action: resumeAction, context_digest: session.context_digest, state_digest: digest({ session: session.id, session_version: session.version, task_run: taskRun?.id ?? null, task_run_version: taskRun?.version ?? null, wait_condition: waitCondition, resume_action: resumeAction }) };` |
| `src/long-task-worker.ts` | `20.0` | 17:395 | 0 | `const checkpointId = String(args.checkpoint_id ?? `long_task_${randomUUID().replaceAll("-", "")}`); const waitCondition = text(args.wait_condition, "wait_condition"); const resumeAction = text(args.resume_action ?? "revalidate_and_resume", "resume_action"); const state = { session_id: session.id, session_version: session.version, task_run_id: taskRun?.id ?? null, task_run_version: taskRun?.version ?? null, host_run_id: args.host_run_id ?? null, wait_condition: waitCondition, resume_action: resumeAction, context_digest: session.context_digest, state_digest: digest({ session: session.id, session_version: session.version, task_run: taskRun?.id ?? null, task_run_version: taskRun?.version ?? null, wait_condition: waitCondition, resume_action: resumeAction }) };` |
| `src/long-task-worker.ts` | `21.0` | 17:648 | 0 | `const checkpointId = String(args.checkpoint_id ?? `long_task_${randomUUID().replaceAll("-", "")}`); const waitCondition = text(args.wait_condition, "wait_condition"); const resumeAction = text(args.resume_action ?? "revalidate_and_resume", "resume_action"); const state = { session_id: session.id, session_version: session.version, task_run_id: taskRun?.id ?? null, task_run_version: taskRun?.version ?? null, host_run_id: args.host_run_id ?? null, wait_condition: waitCondition, resume_action: resumeAction, context_digest: session.context_digest, state_digest: digest({ session: session.id, session_version: session.version, task_run: taskRun?.id ?? null, task_run_version: taskRun?.version ?? null, wait_condition: waitCondition, resume_action: resumeAction }) };` |
| `src/long-task-worker.ts` | `22.0` | 17:687 | 0 | `const checkpointId = String(args.checkpoint_id ?? `long_task_${randomUUID().replaceAll("-", "")}`); const waitCondition = text(args.wait_condition, "wait_condition"); const resumeAction = text(args.resume_action ?? "revalidate_and_resume", "resume_action"); const state = { session_id: session.id, session_version: session.version, task_run_id: taskRun?.id ?? null, task_run_version: taskRun?.version ?? null, host_run_id: args.host_run_id ?? null, wait_condition: waitCondition, resume_action: resumeAction, context_digest: session.context_digest, state_digest: digest({ session: session.id, session_version: session.version, task_run: taskRun?.id ?? null, task_run_version: taskRun?.version ?? null, wait_condition: waitCondition, resume_action: resumeAction }) };` |
| `src/long-task-worker.ts` | `29.0` | 25:178 | 0 | `const checkpoint = this.store.get("long_task_checkpoint", text(args.checkpoint_id, "checkpoint_id")); if (!["waiting", "wake_requested"].includes(String(checkpoint.status))) throw new Error("Long Task checkpoint is not waiting");` |
| `src/long-task-worker.ts` | `30.0` | 26:259 | 0 | `const signal = text(args.signal, "signal"); const saved = this.store.save("long_task_checkpoint", String(checkpoint.id), { ...payload(checkpoint), status: "wake_requested", wake_signal_digest: digest(signal), wake_reason: args.reason === undefined ? null : text(args.reason, "reason"), woken_at: now(args.now) }); return { checkpoint: saved, idempotent: false };` |
| `src/long-task-worker.ts` | `32.0` | 30:178 | 0 | `const checkpoint = this.store.get("long_task_checkpoint", text(args.checkpoint_id, "checkpoint_id")); if (!["wake_requested", "waiting"].includes(String(checkpoint.status))) throw new Error("Long Task checkpoint cannot resume from its current state");` |
| `src/long-task-worker.ts` | `33.0` | 31:131 | 0 | `const session = this.store.find("work_session", String(checkpoint.session_id)); const issues: JsonObject[] = []; if (!session) issues.push({ component: "work_session", issue: "missing" }); else if (Number(session.version) < Number(checkpoint.session_version)) issues.push({ component: "work_session", issue: "version_regressed" }); else if (session.context_digest !== checkpoint.context_digest) issues.push({ component: "work_session", issue: "context_changed" });` |
| `src/long-task-worker.ts` | `34.0` | 31:264 | 0 | `const session = this.store.find("work_session", String(checkpoint.session_id)); const issues: JsonObject[] = []; if (!session) issues.push({ component: "work_session", issue: "missing" }); else if (Number(session.version) < Number(checkpoint.session_version)) issues.push({ component: "work_session", issue: "version_regressed" }); else if (session.context_digest !== checkpoint.context_digest) issues.push({ component: "work_session", issue: "context_changed" });` |
| `src/long-task-worker.ts` | `46.0` | 37:88 | 0 | `list(args: JsonObject = {}): JsonObject { const limit = args.limit === undefined ? 50 : Number(args.limit); if (!Number.isInteger(limit) \|\| limit < 1 \|\| limit > 500) throw new Error("limit must be an integer between 1 and 500"); return { checkpoints: this.store.list("long_task_checkpoint", limit, (item) => args.session_id === undefined \|\| item.session_id === args.session_id) }; }` |
| `src/long-task-worker.ts` | `47.0` | 37:168 | 0 | `list(args: JsonObject = {}): JsonObject { const limit = args.limit === undefined ? 50 : Number(args.limit); if (!Number.isInteger(limit) \|\| limit < 1 \|\| limit > 500) throw new Error("limit must be an integer between 1 and 500"); return { checkpoints: this.store.list("long_task_checkpoint", limit, (item) => args.session_id === undefined \|\| item.session_id === args.session_id) }; }` |
| `src/mcp-http.ts` | `2.0` | 13:50 | 0 | `const buffer = Buffer.isBuffer(chunk) ? chunk : Buffer.from(chunk);` |
| `src/mcp-http.ts` | `19.0` | 42:49 | 0 | `response.statusCode = result === undefined ? 202 : 200;` |
| `src/mcp-http.ts` | `21.0` | 44:40 | 0 | `response.end(result === undefined ? "" : JSON.stringify(result));` |
| `src/mcp-http.ts` | `28.0` | 47:138 | 0 | `response.setHeader("content-type", "application/json"); response.end(JSON.stringify({ error: error instanceof Error ? error.message : String(error) }));` |
| `src/mcp-http.ts` | `34.0` | 55:102 | 0 | `await new Promise<void>((resolve, reject) => { http.once("error", reject); http.listen(options.port ?? 8787, options.host ?? "127.0.0.1", () => { http.removeListener("error", reject); resolve(); }); });` |
| `src/mcp-registry.ts` | `2.0` | 4:102 | 0 | `function text(value: unknown, name: string): string { if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`); return value.trim(); }` |
| `src/mcp-registry.ts` | `10.0` | 13:43 | 0 | `const sourceId = String(args.source_id ?? `mcp_registry_source_${randomUUID().replaceAll("-", "")}`); const endpoint = https(args.endpoint, "endpoint"); const trust = text(args.trust ?? "community", "trust"); if (!TRUST.has(trust)) throw new Error("Unsupported registry trust");` |
| `src/mcp-registry.ts` | `15.0` | 14:252 | 0 | `const identity = { endpoint, trust, key_digest: args.key_digest ?? null }; const identityDigest = digest(identity); const existing = this.store.find("mcp_registry_source", sourceId); if (existing) { if (existing.identity_digest !== identityDigest) throw new Error("MCP Registry source conflict"); return { source: existing, idempotent: true }; }` |
| `src/mcp-registry.ts` | `22.0` | 19:378 | 0 | `const serverId = String(args.server_id ?? `mcp_registry_server_${digest({ source_id: source.id, name, version, digest: digestValue }).slice(-24)}`); const record = { source_id: source.id, name, version, endpoint, digest: digestValue, capabilities: args.capabilities ?? [], status: "active" }; const existing = this.store.find("mcp_registry_server", serverId); if (existing) { if (existing.digest !== digestValue) throw new Error("MCP Registry server digest drift"); return { server: existing, idempotent: true }; }` |
| `src/mcp-registry.ts` | `31.0` | 22:584 | 0 | `health(args: JsonObject): JsonObject { const server = this.store.get("mcp_registry_server", text(args.server_id, "server_id")); const status = text(args.status ?? "healthy", "status"); if (!HEALTH.has(status)) throw new Error("Unsupported MCP Registry health status"); const healthId = String(args.health_id ?? `mcp_registry_health_${server.id}`); const existing = this.store.find("mcp_registry_health", healthId); const result = { server_id: server.id, status, checked_at: args.checked_at ?? new Date().toISOString(), evidence_digest: args.evidence_digest ?? null }; if (existing) return { health: existing, idempotent: true }; return { health: this.store.create("mcp_registry_health", healthId, result), idempotent: false }; }` |
| `src/mcp-registry.ts` | `35.0` | 27:122 | 0 | `const source = this.store.get("mcp_registry_source", text(args.source_id, "source_id")); if (source.enabled !== true) throw new Error("MCP Registry source is disabled");` |
| `src/mcp-registry.ts` | `40.0` | 30:94 | 0 | `const body = await response.json(); const entries: unknown[] \| null = Array.isArray(body) ? body : body && typeof body === "object" && Array.isArray((body as JsonObject).servers) ? (body as JsonObject).servers as unknown[] : null;` |
| `src/memory-governance.ts` | `3.0` | 11:90 | 0 | `function text(v: unknown, name: string): string { if (typeof v !== "string" \|\| !v.trim()) throw new Error(`${name} must not be empty`); return v.trim(); }` |
| `src/memory-governance.ts` | `12.0` | 15:100 | 0 | `function evidenceIds(v: unknown): string[] { if (v === undefined) return []; if (!Array.isArray(v)) throw new Error("evidence_ids must be an array"); const values = v.map((x) => text(x, "evidence_ids")); if (new Set(values).size !== values.length) throw new Error("evidence_ids must be unique"); return values.sort(); }` |
| `src/memory-governance.ts` | `25.0` | 27:109 | 0 | `const confidence = text(args.confidence ?? "unverified", "confidence"); if (!CONFIDENCE.has(confidence)) throw new Error("confidence is unsupported");` |
| `src/memory-governance.ts` | `38.0` | 34:30 | 0 | `if (kind === "procedural" && !ids.length) throw new Error("procedural memory requires Evidence");` |
| `src/memory-governance.ts` | `39.0` | 34:46 | 0 | `if (kind === "procedural" && !ids.length) throw new Error("procedural memory requires Evidence");` |
| `src/memory-governance.ts` | `51.0` | 41:212 | 0 | `for (const conflict of conflicts) this.store.save("memory_candidate", String(conflict.id), { ...payload(conflict), status: "conflict_pending", conflict_ids: [...new Set([...(conflict.conflict_ids as string[] ?? []), candidate.id])] });` |
| `src/memory-governance.ts` | `62.0` | 47:55 | 0 | `if (!new Set(["approve", "reject"]).has(decision)) throw new Error("decision must be approve or reject");` |
| `src/memory-governance.ts` | `75.0` | 65:127 | 0 | `listConflicts(args: JsonObject = {}): JsonObject { return { conflicts: this.store.list("memory_candidate", Number(args.limit ?? 100), (x) => x.status === "conflict_pending") }; }` |
| `src/memory-governance.ts` | `80.0` | 69:163 | 0 | `const reason = noSecret(text(args.reason, "reason"), "reason"); const conflictIds = Array.isArray(candidate.conflict_ids) ? candidate.conflict_ids as string[] : [];` |
| `src/memory-governance.ts` | `85.0` | 71:247 | 0 | `for (const conflictId of conflictIds) { const other = this.store.find("memory_candidate", String(conflictId)); if (other) this.store.save("memory_candidate", String(other.id), { ...payload(other), conflict_ids: (other.conflict_ids as string[] ?? []).filter((x) => x !== candidate.id), status: "candidate" }); }` |
| `src/memory-governance.ts` | `89.0` | 76:48 | 0 | `const now = new Date(args.now === undefined ? Date.now() : text(args.now, "now")); if (Number.isNaN(now.valueOf())) throw new Error("now must be an ISO timestamp"); const expired: JsonObject[] = [];` |
| `src/memory-governance.ts` | `110.0` | 89:57 | 0 | `const consolidationId = String(args.consolidation_id ?? id("memory_consolidation")); const identity = { candidate_ids: ids, summary_digest: digest(noSecret(text(args.summary, "summary"), "summary")) }; const existing = this.store.find("memory_consolidation", consolidationId);` |
| `src/model-gateway.ts` | `47.0` | 99:22 | 0 | `label: model.name \|\| model.id,` |
| `src/model-gateway.ts` | `84.0` | 169:73 | 0 | `if (!["system", "user", "assistant", "tool"].includes(message.role)) throw new Error(`chat message ${index} has an unsupported role`);` |
| `src/model-gateway.ts` | `93.0` | 179:108 | 0 | `const system = messages.filter((message) => message.role === "system").map((message) => message.content ?? "").join("\n\n");` |
| `src/model-gateway.ts` | `96.0` | 182:9 | 0 | `? ({ role: "user", content: [{ type: "tool_result", tool_use_id: message.tool_call_id ?? "unknown", content: message.content ?? "" }] } as JsonObject)` |
| `src/model-gateway.ts` | `97.0` | 183:51 | 0 | `: ({ ...message, ...(message.tool_call_id ? { tool_use_id: message.tool_call_id } : {}) } as JsonObject)),` |
| `src/model-gateway.ts` | `99.0` | 184:210 | 0 | `...(system ? { system } : {}), ...(options.tools?.length ? { tools: options.tools.map((tool) => ({ name: tool.function.name, description: tool.function.description, input_schema: tool.function.parameters ?? { type: "object" } })) } : {}), ...(options.stream ? { stream: true } : {}) };` |
| `src/model-gateway.ts` | `71.0` | 184:264 | 0 | `...(system ? { system } : {}), ...(options.tools?.length ? { tools: options.tools.map((tool) => ({ name: tool.function.name, description: tool.function.description, input_schema: tool.function.parameters ?? { type: "object" } })) } : {}), ...(options.stream ? { stream: true } : {}) };` |
| `src/model-gateway.ts` | `78.0` | 188:169 | 0 | `...(options.temperature === undefined ? {} : { temperature: options.temperature }), ...(options.tools?.length ? { tools: options.tools } : {}), ...(options.stream ? { stream: true } : {}) };` |
| `src/model-gateway.ts` | `142.0` | 202:119 | 0 | `const toolCalls = blocks.filter((block) => block.type === "tool_use").map((block, index) => ({ id: String(block.id ?? `tool_${index + 1}`), type: "function" as const, function: { name: String(block.name), arguments: JSON.stringify(block.input ?? {}) } }));` |
| `src/model-gateway.ts` | `143.0` | 202:247 | 0 | `const toolCalls = blocks.filter((block) => block.type === "tool_use").map((block, index) => ({ id: String(block.id ?? `tool_${index + 1}`), type: "function" as const, function: { name: String(block.name), arguments: JSON.stringify(block.input ?? {}) } }));` |
| `src/model-gateway.ts` | `145.0` | 212:32 | 0 | `return { id: String(item.id ?? `tool_${index + 1}`), type: "function" as const, function: { name: String(fn.name), arguments: typeof fn.arguments === "string" ? fn.arguments : JSON.stringify(fn.arguments ?? {}) } };` |
| `src/model-gateway.ts` | `146.0` | 212:163 | 0 | `return { id: String(item.id ?? `tool_${index + 1}`), type: "function" as const, function: { name: String(fn.name), arguments: typeof fn.arguments === "string" ? fn.arguments : JSON.stringify(fn.arguments ?? {}) } };` |
| `src/model-gateway.ts` | `147.0` | 212:208 | 0 | `return { id: String(item.id ?? `tool_${index + 1}`), type: "function" as const, function: { name: String(fn.name), arguments: typeof fn.arguments === "string" ? fn.arguments : JSON.stringify(fn.arguments ?? {}) } };` |
| `src/oidc-jwks.ts` | `13.0` | 29:69 | 0 | `if (!value \|\| typeof value !== "object" \|\| Array.isArray(value)) throw new Error();` |
| `src/oidc-jwks.ts` | `31.0` | 56:113 | 0 | `const endpoint = httpsUrl(options.jwksUrl ?? jwksEndpoint(issuer), "jwksUrl"); const fetchImpl = options.fetch ?? fetch as unknown as FetchLike;` |
| `src/oidc-jwks.ts` | `32.0` | 57:26 | 0 | `const now = options.now ?? Date.now; const cacheTtlMs = options.cacheTtlMs ?? 300_000;` |
| `src/oidc-jwks.ts` | `35.0` | 58:55 | 0 | `if (!Number.isInteger(cacheTtlMs) \|\| cacheTtlMs < 1) throw new Error("cacheTtlMs must be a positive integer");` |
| `src/oidc-jwks.ts` | `54.0` | 74:75 | 0 | `const parts = text(token, "token").split("."); if (parts.length !== 3) throw new Error("OIDC token must be a compact JWT");` |
| `src/oidc-jwks.ts` | `60.0` | 80:4 | 0 | `catch { throw new Error("OIDC token key is invalid"); }` |
| `src/org-sync.ts` | `2.0` | 4:102 | 0 | `function text(value: unknown, name: string): string { if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`); return value.trim(); }` |
| `src/org-sync.ts` | `5.0` | 6:84 | 0 | `function array(value: unknown, name: string): string[] { if (!Array.isArray(value)) throw new Error(`${name} must be an array`); return value.map((item) => text(item, name)); }` |
| `src/org-sync.ts` | `13.0` | 10:589 | 0 | `prepare(args: JsonObject): JsonObject { const workspaceId = text(args.workspace_id, "workspace_id"); const memberIds = array(args.member_ids ?? [], "member_ids"); const recordRefs = array(args.record_refs ?? [], "record_refs"); const manifest = { workspace_id: workspaceId, member_ids: memberIds, record_refs: recordRefs, encryption: "adapter_managed", deletion_tombstones: true }; const manifestDigest = digest(manifest); const syncId = String(args.sync_id ?? `org_sync_${randomUUID().replaceAll("-", "")}`); const existing = this.store.find("org_sync_manifest", syncId); if (existing) { if (existing.manifest_digest !== manifestDigest) throw new Error("Organization sync manifest conflict"); return { manifest: existing, idempotent: true }; } return { manifest: this.store.create("org_sync_manifest", syncId, { ...manifest, manifest_digest: manifestDigest, status: "prepared" }), idempotent: false }; }` |
| `src/org-sync.ts` | `15.0` | 11:266 | 0 | `apply(args: JsonObject): JsonObject { const manifest = this.store.get("org_sync_manifest", text(args.sync_id, "sync_id")); const baseDigest = text(args.base_digest, "base_digest"); const currentDigest = args.current_digest === undefined ? manifest.manifest_digest : text(args.current_digest, "current_digest"); if (currentDigest !== baseDigest) return { status: "conflict", conflict: { expected: baseDigest, actual: currentDigest }, idempotent: false }; const applied = this.store.save("org_sync_manifest", String(manifest.id), { ...manifest, status: "applied", applied_at: new Date().toISOString(), tombstone_digest: digest(args.tombstones ?? []) }); return { status: "applied", manifest: applied, idempotent: false }; }` |
| `src/os-security.ts` | `2.0` | 5:102 | 0 | `function text(value: unknown, name: string): string { if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`); return value.trim(); }` |
| `src/os-security.ts` | `7.0` | 7:119 | 0 | `function list(value: unknown, name: string): string[] { if (value === undefined) return []; if (!Array.isArray(value)) throw new Error(`${name} must be an array`); return value.map((item) => text(item, name)); }` |
| `src/os-security.ts` | `17.0` | 19:90 | 0 | `const network = text(args.network ?? "denied", "network"); if (!NETWORK.has(network)) throw new Error("Unsupported network policy");` |
| `src/os-security.ts` | `20.0` | 20:108 | 0 | `const filesystem = text(args.filesystem ?? "read_only", "filesystem"); if (!FILESYSTEM.has(filesystem)) throw new Error("Unsupported filesystem policy");` |
| `src/os-security.ts` | `26.0` | 23:80 | 0 | `const boundaryDigest = digest(boundary); const planId = String(args.plan_id ?? `os_security_${randomUUID().replaceAll("-", "")}`); const existing = this.store.find("os_security_plan", planId);` |
| `src/os-security.ts` | `28.0` | 24:69 | 0 | `if (existing) { if (existing.boundary_digest !== boundaryDigest) throw new Error("OS security plan idempotency conflict"); return { plan: existing, idempotent: true }; }` |
| `src/os-security.ts` | `31.0` | 29:217 | 0 | `const plan = this.store.get("os_security_plan", text(args.plan_id, "plan_id")); const observed = (args.observed && typeof args.observed === "object" && !Array.isArray(args.observed)) ? args.observed as JsonObject : {};` |
| `src/os-security.ts` | `32.0` | 30:66 | 0 | `const observedDigest = observed.boundary_digest === undefined ? null : text(observed.boundary_digest, "observed.boundary_digest");` |
| `src/os-security.ts` | `35.0` | 33:18 | 0 | `if (existing) return { receipt: existing, compatible: existing.compatible === true, idempotent: true };` |
| `src/project-brain.ts` | `2.0` | 5:50 | 0 | `if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`);` |
| `src/project-brain.ts` | `6.0` | 10:29 | 0 | `if (!Array.isArray(value)) throw new Error(`${name} must be an array`);` |
| `src/project-brain.ts` | `8.0` | 12:46 | 0 | `if (new Set(result).size !== result.length) throw new Error(`${name} must contain unique values`);` |
| `src/project-brain.ts` | `30.0` | 44:16 | 0 | `if (!brain) throw new Error(`Unknown Project Brain: ${project}`);` |
| `src/project-brain.ts` | `57.0` | 62:65 | 0 | `const record = { project_id: project, title: text(args.title ?? previous?.title, "title"), status: String(args.status ?? previous?.status ?? "active"), constraint_digests: list(args.constraint_digests ?? previous?.constraint_digests, "constraint_digests"), metric: args.metric === undefined ? previous?.metric ?? null : text(args.metric, "metric"), content_digest: digest({ title: args.title ?? previous?.title, constraints: args.constraint_digests ?? previous?.constraint_digests ?? [], metric: args.metric ?? previous?.metric ?? null }) };` |
| `src/project-brain.ts` | `64.0` | 62:305 | 0 | `const record = { project_id: project, title: text(args.title ?? previous?.title, "title"), status: String(args.status ?? previous?.status ?? "active"), constraint_digests: list(args.constraint_digests ?? previous?.constraint_digests, "constraint_digests"), metric: args.metric === undefined ? previous?.metric ?? null : text(args.metric, "metric"), content_digest: digest({ title: args.title ?? previous?.title, constraints: args.constraint_digests ?? previous?.constraint_digests ?? [], metric: args.metric ?? previous?.metric ?? null }) };` |
| `src/project-brain.ts` | `66.0` | 62:396 | 0 | `const record = { project_id: project, title: text(args.title ?? previous?.title, "title"), status: String(args.status ?? previous?.status ?? "active"), constraint_digests: list(args.constraint_digests ?? previous?.constraint_digests, "constraint_digests"), metric: args.metric === undefined ? previous?.metric ?? null : text(args.metric, "metric"), content_digest: digest({ title: args.title ?? previous?.title, constraints: args.constraint_digests ?? previous?.constraint_digests ?? [], metric: args.metric ?? previous?.metric ?? null }) };` |
| `src/project-brain.ts` | `71.0` | 62:523 | 0 | `const record = { project_id: project, title: text(args.title ?? previous?.title, "title"), status: String(args.status ?? previous?.status ?? "active"), constraint_digests: list(args.constraint_digests ?? previous?.constraint_digests, "constraint_digests"), metric: args.metric === undefined ? previous?.metric ?? null : text(args.metric, "metric"), content_digest: digest({ title: args.title ?? previous?.title, constraints: args.constraint_digests ?? previous?.constraint_digests ?? [], metric: args.metric ?? previous?.metric ?? null }) };` |
| `src/project-brain.ts` | `81.0` | 75:90 | 0 | `const previous = this.store.find("project_material", id); return { material: previous ? this.store.save("project_material", id, { ...payload(previous), ...material }) : this.store.create("project_material", id, material), idempotent: false };` |
| `src/project-brain.ts` | `84.0` | 79:246 | 0 | `const project = projectId(args.project_id); this.require(project); const id = String(args.outcome_id ?? `project_outcome_${randomUUID().replaceAll("-", "")}`); const outcome = { project_id: project, task_id: args.task_id === undefined ? null : text(args.task_id, "task_id"), session_id: args.session_id === undefined ? null : text(args.session_id, "session_id"), verdict: text(args.verdict, "verdict"), summary_digest: digest(text(args.summary, "summary")), evidence_ids: list(args.evidence_ids, "evidence_ids"), artifact_ids: list(args.artifact_ids, "artifact_ids"), status: "recorded" };` |
| `src/project-knowledge.ts` | `8.0` | 12:47 | 0 | `if (relative(root, target).startsWith("..")) throw new Error("Project Knowledge path escapes the trusted project root");` |
| `src/project-knowledge.ts` | `28.0` | 29:33 | 0 | `if (stat.isSymbolicLink()) throw new Error("Project Knowledge does not follow symbolic links");` |
| `src/remote-runtime.ts` | `6.0` | 12:32 | 0 | `if (!Number.isFinite(result)) throw new Error(`${name} must be an ISO timestamp`);` |
| `src/remote-runtime.ts` | `21.0` | 59:55 | 0 | `if (existing.identity_digest !== identityDigest) throw new Error("Remote tenant idempotency conflict");` |
| `src/remote-runtime.ts` | `23.0` | 70:39 | 0 | `const now = args.now === undefined ? Date.now() : instant(args.now, "now");` |
| `src/remote-runtime.ts` | `27.0` | 78:45 | 0 | `const bindingId = String(args.binding_id ?? `remote_task_${randomBytes(12).toString("hex")}`);` |
| `src/remote-runtime.ts` | `29.0` | 81:53 | 0 | `if (existing.binding_digest !== bindingDigest) throw new Error("Remote task binding idempotency conflict");` |
| `src/remote-runtime.ts` | `33.0` | 96:39 | 0 | `const now = args.now === undefined ? Date.now() : instant(args.now, "now");` |
| `src/remote-runtime.ts` | `42.0` | 101:96 | 0 | `if (this.sha256Ref(args.principal_digest, "principal_digest") !== binding.principal_digest) throw new Error("Remote task principal does not match");` |
| `src/remote-runtime.ts` | `44.0` | 102:111 | 0 | `if (this.sha256Ref(args.access_receipt_digest, "access_receipt_digest") !== binding.access_receipt_digest) throw new Error("Remote task authorization receipt does not match");` |
| `src/remote-runtime.ts` | `46.0` | 103:62 | 0 | `if (text(args.audience, "audience") !== binding.audience) throw new Error("Remote task audience does not match");` |
| `src/remote-runtime.ts` | `50.0` | 106:45 | 0 | `const receiptId = String(args.receipt_id ?? `remote_task_access_${binding.id}_${operation}_${randomBytes(8).toString("hex")}`);` |
| `src/remote-runtime.ts` | `52.0` | 110:55 | 0 | `if (existing.access_digest !== digest(identity)) throw new Error("Remote task access receipt idempotency conflict");` |
| `src/remote-runtime.ts` | `56.0` | 120:37 | 0 | `if (binding.status !== "active") throw new Error("Only an active remote task binding can be revoked");` |
| `src/remote-runtime.ts` | `58.0` | 121:181 | 0 | `const saved = this.store.save("remote_task_binding", String(binding.id), { ...payload(binding), status: "revoked", revoked_at: args.now === undefined ? new Date().toISOString() : new Date(instant(args.now, "now")).toISOString(), revocation_reason: text(args.reason, "reason") });` |
| `src/runtime-acceptance.ts` | `12.0` | 10:204 | 0 | `const values = value.map((item) => text(item, name)); if (new Set(values).size !== values.length \|\| exact !== undefined && values.length !== exact) throw new Error(`${name} must contain exactly ${exact ?? "unique"} values`);` |
| `src/runtime-acceptance.ts` | `16.0` | 13:192 | 0 | `function integer(value: unknown, name: string, minimum: number, maximum: number): number { const result = Number(value); if (!Number.isInteger(result) \|\| result < minimum \|\| result > maximum) throw new Error(`${name} must be an integer between ${minimum} and ${maximum}`); return result; }` |
| `src/runtime-acceptance.ts` | `22.0` | 30:39 | 0 | `const planId = String(args.plan_id ?? `runtime_acceptance_${randomUUID().replaceAll("-", "")}`); const existing = this.store.find("runtime_acceptance_plan", planId); const planDigest = digest(identity);` |
| `src/runtime-acceptance.ts` | `23.0` | 31:18 | 0 | `if (existing) { if (existing.plan_digest !== planDigest) throw new Error("Runtime acceptance plan idempotency conflict"); return { plan: existing, idempotent: true }; }` |
| `src/runtime-acceptance.ts` | `30.0` | 38:101 | 0 | `const caseId = text(args.case_id, "case_id"); if (!(plan.case_ids as string[]).includes(caseId)) throw new Error("Runtime acceptance Case is not in the plan");` |
| `src/runtime-acceptance.ts` | `42.0` | 43:199 | 0 | `const session = this.store.get("host_session", text(args.host_session_id, "host_session_id")); if (session.host_id !== hostId \|\| session.environment_fingerprint !== plan.environment_fingerprint) throw new Error("Runtime acceptance Host Session does not match");` |
| `src/runtime-acceptance.ts` | `48.0` | 48:18 | 0 | `if (existing) { if (existing.record_digest !== recordDigest) throw new Error("Runtime acceptance record idempotency conflict"); return { record: existing, idempotent: true }; }` |
| `src/runtime-acceptance.ts` | `50.0` | 50:19 | 0 | `if (duplicate) throw new Error("Runtime acceptance slot is already recorded");` |
| `src/runtime-acceptance.ts` | `62.0` | 63:61 | 0 | `if (candidatePassed && !basePassed) candidateWins += 1; else if (basePassed && !candidatePassed) baselineWins += 1; else ties += 1;` |
| `src/runtime-acceptance.ts` | `68.0` | 66:73 | 0 | `const status = !complete ? "inconclusive" : strictProof ? "eligible" : baselineWins >= candidateWins ? "rejected" : "inconclusive";` |
| `src/runtime-acceptance.ts` | `70.0` | 69:18 | 0 | `if (existing) { if (existing.evaluation_digest !== evaluationDigest) throw new Error("Runtime acceptance evaluation idempotency conflict"); return { evaluation: existing, idempotent: true }; }` |
| `src/runtime-acceptance.ts` | `83.0` | 75:362 | 0 | `get(args: JsonObject): JsonObject { const plan = this.store.get("runtime_acceptance_plan", text(args.plan_id, "plan_id")); return { plan, records: this.store.list("runtime_acceptance_record", 10_000, (item) => item.plan_id === plan.id), evaluation: plan.latest_evaluation_id ? this.store.get("runtime_acceptance_evaluation", String(plan.latest_evaluation_id)) : null }; }` |
| `src/runtime-assurance.ts` | `21.0` | 42:54 | 0 | `const environmentDigest = digest(args.environment ?? {}); const budgetDigest = digest(args.budget ?? {});` |
| `src/runtime-assurance.ts` | `22.0` | 42:102 | 0 | `const environmentDigest = digest(args.environment ?? {}); const budgetDigest = digest(args.budget ?? {});` |
| `src/runtime-assurance.ts` | `27.0` | 48:66 | 0 | `if (!(contract.allowed_effects as string[]).includes(effect)) throw new Error("Runtime Assurance effect is not allowed by the Task Contract");` |
| `src/runtime-assurance.ts` | `31.0` | 53:35 | 0 | `const delivery = deliveries[0] ?? null;` |
| `src/runtime-assurance.ts` | `34.0` | 55:39 | 0 | `: hostRun.status !== "completed" ? "rejected"` |
| `src/runtime-assurance.ts` | `36.0` | 57:23 | 0 | `? "verified" : "needs_review";` |
| `src/runtime-assurance.ts` | `39.0` | 62:57 | 0 | `reobservation, boundary, delivery_id: delivery?.id ?? null, delivery_version: delivery?.version ?? null,` |
| `src/runtime-assurance.ts` | `41.0` | 62:102 | 0 | `reobservation, boundary, delivery_id: delivery?.id ?? null, delivery_version: delivery?.version ?? null,` |
| `src/runtime-assurance.ts` | `44.0` | 67:75 | 0 | `if (existing) { if (existing.attestation_digest !== attestationDigest) throw new Error("Runtime Assurance attestation idempotency conflict"); return { attestation: existing, idempotent: true }; }` |
| `src/runtime-assurance.ts` | `50.0` | 76:102 | 0 | `const evidenceIds = ids(args.evidence_ids, "evidence_ids"); for (const evidenceId of evidenceIds) this.store.get("evidence", evidenceId);` |
| `src/runtime-assurance.ts` | `52.0` | 81:18 | 0 | `if (existing) { if (existing.intervention_digest !== interventionDigest) throw new Error("Runtime Assurance intervention idempotency conflict"); return { intervention: existing, idempotent: true }; }` |
| `src/runtime-assurance.ts` | `63.0` | 96:23 | 0 | `if (!matches[0]) throw new Error("Runtime Assurance Campaign requires a verified attestation for every bound Task Run");` |
| `src/runtime-assurance.ts` | `58.0` | 103:63 | 0 | `if (existing.assurance_campaign_digest !== recordDigest) throw new Error("Runtime Assurance Campaign idempotency conflict");` |
| `src/runtime-assurance.ts` | `59.0` | 108:63 | 0 | `status: (result.runner as JsonObject).campaign_lifecycle ?? "collecting" });` |
| `src/runtime-assurance.ts` | `77.0` | 133:50 | 0 | `if (workspace.root_path !== launch.workspace) throw new Error("Runtime Assurance Workspace observation is outside the Task Run workspace");` |
| `src/runtime-truth-kernel.ts` | `2.0` | 6:102 | 0 | `function text(value: unknown, name: string): string { if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`); return value.trim(); }` |
| `src/runtime-truth-kernel.ts` | `4.0` | 7:125 | 0 | `function object(value: unknown, name: string): JsonObject { if (!value \|\| typeof value !== "object" \|\| Array.isArray(value)) throw new Error(`${name} must be an object`); return value as JsonObject; }` |
| `src/runtime-truth-kernel.ts` | `8.0` | 15:53 | 0 | `const trace = standardizeTrace(object(args.trace ?? args, "trace"));` |
| `src/runtime-truth-kernel.ts` | `9.0` | 16:35 | 0 | `const id = text(args.export_id ?? `trace_export_${randomUUID().replaceAll("-", "")}`, "export_id");` |
| `src/runtime-truth-kernel.ts` | `13.0` | 23:53 | 0 | `const trace = standardizeTrace(object(args.trace ?? args, "trace"));` |
| `src/runtime-truth-kernel.ts` | `16.0` | 24:79 | 0 | `const events = args.events === undefined ? [] : Array.isArray(args.events) ? args.events as JsonObject[] : (() => { throw new Error("events must be an array"); })();` |
| `src/runtime-truth-kernel.ts` | `22.0` | 34:35 | 0 | `const id = text(args.export_id ?? `trace_otlp_${randomUUID().replaceAll("-", "")}`, "export_id");` |
| `src/runtime-truth-kernel.ts` | `27.0` | 43:34 | 0 | `if (!Array.isArray(messages)) throw new Error("messages must be an array");` |
| `src/runtime-truth-kernel.ts` | `28.0` | 44:115 | 0 | `const result = compactConversation(messages as ConversationMessage[], args.max_chars === undefined ? undefined : Number(args.max_chars));` |
| `src/runtime-truth-kernel.ts` | `29.0` | 45:36 | 0 | `const id = text(args.session_id ?? `context_${randomUUID().replaceAll("-", "")}`, "session_id");` |
| `src/runtime-truth-kernel.ts` | `31.0` | 52:33 | 0 | `const id = text(args.note_id ?? `work_note_${randomUUID().replaceAll("-", "")}`, "note_id");` |
| `src/runtime-truth.ts` | `6.0` | 28:67 | 0 | `if (!value \|\| typeof value !== "object" \|\| Array.isArray(value)) throw new Error(`${name} must be an object`);` |
| `src/runtime-truth.ts` | `40.0` | 64:68 | 0 | `baggage: { task_id: input.task_id ?? null, run_id: input.run_id ?? null, operation_id: input.operation_id ?? null } };` |
| `src/runtime-truth.ts` | `41.0` | 64:110 | 0 | `baggage: { task_id: input.task_id ?? null, run_id: input.run_id ?? null, operation_id: input.operation_id ?? null } };` |
| `src/runtime-truth.ts` | `56.0` | 100:33 | 0 | `try { parsed = new URL(url); } catch { throw new Error("endpoint must be a valid HTTP(S) URL"); }` |
| `src/runtime-truth.ts` | `80.0` | 129:74 | 0 | `const name = text(fn.name, "tool name"); const rawArgs = fn.arguments ?? {};` |
| `src/runtime-truth.ts` | `110.0` | 150:108 | 0 | `const tail: ConversationMessage[] = []; let used = system.reduce((sum, item) => sum + String(item.content ?? "").length, 0);` |
| `src/runtime-truth.ts` | `100.0` | 152:63 | 0 | `const item = messages[index]!; if (item.role === "system") continue;` |
| `src/runtime-truth.ts` | `101.0` | 153:35 | 0 | `if (used + String(item.content ?? "").length > Math.floor(maxChars * 0.65)) break;` |
| `src/runtime-truth.ts` | `104.0` | 154:52 | 0 | `tail.unshift(item); used += String(item.content ?? "").length;` |
| `src/service-foundation.ts` | `8.0` | 314:32 | 0 | `: configuredModels.length ? configuredModels : PROVIDER_CATALOG;` |
| `src/settings.ts` | `22.0` | 63:2 | 0 | `catch { throw new Error(`${name} must be a valid HTTP(S) URL`); }` |
| `src/settings.ts` | `33.0` | 73:66 | 0 | `if (!item \|\| typeof item !== "object" \|\| Array.isArray(item)) throw new Error(`models[${index}] must be an object`);` |
| `src/settings.ts` | `78.0` | 135:39 | 0 | `models: patch.models !== undefined ? patch.models : current.models,` |
| `src/state-workspace.ts` | `12.0` | 13:47 | 0 | `if (relative(root, target).startsWith("..")) throw new Error("workspace state path escapes root");` |
| `src/store.ts` | `9.0` | 25:29 | 0 | `return Number(row?.value ?? 0);` |
| `src/supervisor.ts` | `76.0` | 49:48 | 0 | `try { return { state: await this.start(port), owned: true }; }` |
| `src/supply-chain-attestation.ts` | `8.0` | 10:134 | 0 | `function sha256(value: unknown, name: string): string { const result = text(value, name); if (!/^sha256:[a-f0-9]{64}$/u.test(result)) throw new Error(`${name} must be a SHA-256 digest`); return result; }` |
| `src/supply-chain-attestation.ts` | `12.0` | 25:4 | 0 | `catch { throw new Error("publisher public_key_pem is invalid"); }` |
| `src/supply-chain-attestation.ts` | `14.0` | 28:18 | 0 | `if (existing) { if (existing.definition_digest !== definitionDigest) throw new Error("Supply-chain publisher idempotency conflict"); return { publisher: existing, idempotent: true }; }` |
| `src/supply-chain-attestation.ts` | `17.0` | 33:140 | 0 | `const publisher = this.store.get("supply_chain_publisher", text(args.publisher_id, "publisher_id")); if (publisher.status !== "active") throw new Error("Supply-chain publisher is not active");` |
| `src/supply-chain-attestation.ts` | `20.0` | 38:4 | 0 | `catch { throw new Error("Supply-chain signature is invalid"); }` |
| `src/supply-chain-attestation.ts` | `22.0` | 39:19 | 0 | `if (!verified) throw new Error("Supply-chain signature does not verify");` |
| `src/supply-chain-attestation.ts` | `24.0` | 41:53 | 0 | `const attestationId = String(args.attestation_id ?? `supply_attestation_${digest(identity).slice(-24)}`); const existing = this.store.find("supply_chain_attestation", attestationId); const attestationDigest = digest(identity);` |
| `src/supply-chain-attestation.ts` | `25.0` | 42:18 | 0 | `if (existing) { if (existing.attestation_digest !== attestationDigest) throw new Error("Supply-chain attestation idempotency conflict"); return { attestation: existing, idempotent: true }; }` |
| `src/supply-chain-attestation.ts` | `28.0` | 47:152 | 0 | `const attestation = this.store.get("supply_chain_attestation", text(args.attestation_id, "attestation_id")); if (attestation.status !== "verified") throw new Error("Supply-chain attestation is not active");` |
| `src/supply-chain-attestation.ts` | `33.0` | 54:151 | 0 | `const attestation = this.store.get("supply_chain_attestation", text(args.attestation_id, "attestation_id")); if (attestation.status === "revoked") return { attestation, idempotent: true };` |
| `src/task-state.ts` | `2.0` | 5:90 | 0 | `function text(v: unknown, name: string): string { if (typeof v !== "string" \|\| !v.trim()) throw new Error(`${name} must not be empty`); return v.trim(); }` |
| `src/task-state.ts` | `10.0` | 14:140 | 0 | `const current = this.store.find("task_state_projection", taskId); const expected = args.expected_revision === undefined ? Number(current?.state_revision ?? 0) : Number(args.expected_revision); if (!Number.isInteger(expected) \|\| expected < 0) throw new Error("expected_revision must be a non-negative integer");` |
| `src/trace-kernel.ts` | `2.0` | 18:50 | 0 | `if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`);` |
| `src/trace-kernel.ts` | `16.0` | 29:29 | 0 | `if (!Array.isArray(value)) throw new Error(`${name} must be an array`);` |
| `src/trace-kernel.ts` | `25.0` | 37:46 | 0 | `if (!Number.isFinite(result) \|\| result < 0) throw new Error(`${name} must be a non-negative finite number`);` |
| `src/trace-kernel.ts` | `65.0` | 88:64 | 0 | `const trust = String(args.trust ?? (args.source === "human" ? "human" : "untrusted")) as TraceTrust;` |
| `src/trace-kernel.ts` | `95.0` | 115:70 | 0 | `if (existing.signal !== signal \|\| existing.summary !== summary) throw new Error("Trace feedback idempotency conflict");` |
| `src/trace-kernel.ts` | `101.0` | 119:168 | 0 | `const event = this.append({ trace_id: traceId, event_kind: `feedback.${signal}`, actor: record.actor, source: "feedback", trust: record.actor === "human" ? "human" : "untrusted", summary, data: {}, output_refs: [record.id] });` |
| `src/trace-kernel.ts` | `103.0` | 125:31 | 0 | `if (!TERMINAL.has(status)) throw new Error("Trace final status is unsupported");` |
| `src/trace-kernel.ts` | `110.0` | 135:77 | 0 | `const archive = this.store.find("trace_archive", traceId); if (!archive) throw new Error(`Unknown trace: ${traceId}`);` |
| `src/trace-kernel.ts` | `136.0` | 144:177 | 0 | `const archived: JsonObject[] = this.store.list("trace_archive", Number.MAX_SAFE_INTEGER, (archive) => (traceId === null \|\| archive.trace_id === traceId) && (taskId === null \|\| archive.task_id === taskId)).flatMap((archive): JsonObject[] => {` |
| `src/trace-kernel.ts` | `139.0` | 146:64 | 0 | `return bundle.events.filter((event) => eventKind === null \|\| event.event_kind === eventKind).map((event) => ({ ...event, archived: true, archive_uri: archive.archive_uri }));` |
| `src/trace-kernel.ts` | `142.0` | 148:76 | 0 | `const events = [...hot, ...archived].sort((a, b) => String(a.created_at ?? "").localeCompare(String(b.created_at ?? ""))).slice(0, limit);` |
| `src/trace-kernel.ts` | `143.0` | 148:117 | 0 | `const events = [...hot, ...archived].sort((a, b) => String(a.created_at ?? "").localeCompare(String(b.created_at ?? ""))).slice(0, limit);` |
| `src/trace-kernel.ts` | `152.0` | 158:156 | 0 | `const result = this.get({ trace_id: args.trace_id }); const trace = result.trace as JsonObject; if (!TERMINAL.has(String(trace.status) as TraceStatus)) throw new Error("Trace Case requires a terminal Trace");` |
| `src/trace-kernel.ts` | `156.0` | 160:61 | 0 | `const approvedBy = args.approved_by === undefined ? null : text(args.approved_by, "approved_by"); if (partition === "held_out" && approvedBy === null) throw new Error("held_out Trace Case requires approved_by");` |
| `src/trace-kernel.ts` | `162.0` | 162:61 | 0 | `if (existing) { if (existing.case_digest !== caseDigest) throw new Error("Trace Case idempotency conflict"); return { case: existing, idempotent: true }; }` |
| `src/trace-kernel.ts` | `174.0` | 168:60 | 0 | `if (args.replace !== undefined && args.replace !== true && args.replace !== false) throw new Error("replace must be a boolean");` |
| `src/trace-kernel.ts` | `175.0` | 168:87 | 0 | `if (args.replace !== undefined && args.replace !== true && args.replace !== false) throw new Error("replace must be a boolean");` |
| `src/trace-kernel.ts` | `183.0` | 169:552 | 0 | `const existing = this.store.find("trace_policy", policyId); const identity = { max_days: maxDays, max_events: maxEvents, pii_mode: String(args.pii_mode ?? "digest_only") }; if (existing) { if (existing.identity_digest === digest(identity)) return { policy: existing, idempotent: true }; if (args.replace !== true) throw new Error("Trace policy idempotency conflict"); return { policy: this.store.save("trace_policy", policyId, { ...payload(existing), ...identity, identity_digest: digest(identity), policy_revision: Number(existing.policy_revision ?? 1) + 1, updated_by: String(args.updated_by ?? "operator") }), idempotent: false }; }` |
| `src/trace-kernel.ts` | `205.0` | 183:63 | 0 | `.filter((trace) => Date.parse(String(trace.last_event_at ?? trace.updated_at ?? trace.started_at)) < cutoff)` |
| `src/trace-kernel.ts` | `206.0` | 183:83 | 0 | `.filter((trace) => Date.parse(String(trace.last_event_at ?? trace.updated_at ?? trace.started_at)) < cutoff)` |
| `src/trace-kernel.ts` | `198.0` | 189:31 | 0 | `const archive = existing ? this.archivePointer(existing) : this.archives.write({ trace_id: String(trace.id), trace_version: Number(trace.version), archived_at: now, trace, events, feedback });` |
| `src/trace-kernel.ts` | `200.0` | 190:225 | 0 | `if (!existing) this.store.create("trace_archive", String(trace.id), { trace_id: trace.id, trace_version: trace.version, task_id: trace.task_id, status: trace.status, archived_at: now, last_event_at: trace.last_event_at ?? trace.updated_at, event_count: events.length, feedback_count: feedback.length, archive_backend_id: archive.backend_id ?? "local", archive_storage: archive.storage, archive_locator: archive.locator, archive_uri: archive.uri, archive_format: archive.format, content_digest: archive.content_digest, bytes: archive.bytes });` |
| `src/trace-kernel.ts` | `201.0` | 190:346 | 0 | `if (!existing) this.store.create("trace_archive", String(trace.id), { trace_id: trace.id, trace_version: trace.version, task_id: trace.task_id, status: trace.status, archived_at: now, last_event_at: trace.last_event_at ?? trace.updated_at, event_count: events.length, feedback_count: feedback.length, archive_backend_id: archive.backend_id ?? "local", archive_storage: archive.storage, archive_locator: archive.locator, archive_uri: archive.uri, archive_format: archive.format, content_digest: archive.content_digest, bytes: archive.bytes });` |
| `src/trace-kernel.ts` | `217.0` | 203:57 | 0 | `return { ...(record.archive_backend_id === undefined ? {} : { backend_id: text(record.archive_backend_id, "archive_backend_id") }), storage: String(record.archive_storage) as TraceArchivePointer["storage"], format: String(record.archive_format) as TraceArchivePointer["format"], locator: text(record.archive_locator, "archive_locator"), uri: text(record.archive_uri, "archive_uri"), content_digest: text(record.content_digest, "archive content_digest"), bytes: Number(record.bytes) };` |
| `src/trace-kernel.ts` | `222.0` | 209:69 | 0 | `if (stored) return { id, revision: Number(stored.policy_revision ?? 1), maxDays: Number(stored.max_days), source: "stored" };` |
| `src/trace-kernel.ts` | `224.0` | 210:26 | 0 | `if (id !== "default") throw new Error("Unknown Trace retention policy");` |
| `src/trust-profile.ts` | `2.0` | 5:50 | 0 | `if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`);` |
| `src/trust-profile.ts` | `6.0` | 10:53 | 0 | `if (!Number.isInteger(number) \|\| number < minimum) throw new Error(`${name} must be an integer >= ${minimum}`);` |
| `src/trust-profile.ts` | `10.0` | 19:27 | 0 | `if (value === undefined) return [];` |
| `src/trust-profile.ts` | `11.0` | 20:29 | 0 | `if (!Array.isArray(value)) throw new Error(`${name} must be an array`);` |
| `src/trust-profile.ts` | `12.0` | 22:46 | 0 | `if (new Set(result).size !== result.length) throw new Error(`${name} must contain unique values`);` |
| `src/trust-profile.ts` | `28.0` | 66:50 | 0 | `const passRate = attempts ? passed / attempts : 0;` |
| `src/trust-profile.ts` | `30.0` | 71:66 | 0 | `if (revoked \|\| expired) { level = "blocked"; reason = revoked ? "profile revoked" : "profile expired"; }` |
| `src/trust-profile.ts` | `52.0` | 99:56 | 0 | `project_id: input.project_id === undefined ? null : text(input.project_id, "scope.project_id"),` |
| `src/trust-profile.ts` | `53.0` | 102:32 | 0 | `effect: text(input.effect ?? "read_only", "scope.effect"), data_scope: text(input.data_scope ?? "project", "scope.data_scope"),` |
| `src/trust-profile.ts` | `54.0` | 102:99 | 0 | `effect: text(input.effect ?? "read_only", "scope.effect"), data_scope: text(input.data_scope ?? "project", "scope.data_scope"),` |
| `src/v01211-runtime.ts` | `10.0` | 14:44 | 0 | `if (new Set(items).size !== items.length) throw new Error(`${name} must contain unique values`);` |
| `src/v01211-runtime.ts` | `14.0` | 19:67 | 0 | `if (!value \|\| typeof value !== "object" \|\| Array.isArray(value)) throw new Error(`${name} must be an object`);` |
| `src/v01211-runtime.ts` | `53.0` | 89:24 | 0 | `if (!events.length) throw new Error("Trace has no replayable action contracts");` |
| `src/v01211-runtime.ts` | `66.0` | 104:38 | 0 | `if (replay.status !== "prepared") throw new Error("Replay is not prepared");` |
| `src/v01211-runtime.ts` | `82.0` | 127:45 | 0 | `const serviceId = String(args.service_id ?? "craft-local");` |
| `src/v01211-runtime.ts` | `89.0` | 135:45 | 0 | `const serviceId = String(args.service_id ?? "craft-local"); const service = this.store.find("local_runtime_service", serviceId) ?? (this.configure({ service_id: serviceId }).service as JsonObject);` |
| `src/v01211-runtime.ts` | `90.0` | 135:132 | 0 | `const serviceId = String(args.service_id ?? "craft-local"); const service = this.store.find("local_runtime_service", serviceId) ?? (this.configure({ service_id: serviceId }).service as JsonObject);` |
| `src/v01211-runtime.ts` | `94.0` | 141:45 | 0 | `const serviceId = String(args.service_id ?? "craft-local"); const service = this.store.get("local_runtime_service", serviceId);` |
| `src/v01211-runtime.ts` | `98.0` | 147:45 | 0 | `const serviceId = String(args.service_id ?? "craft-local"); const service = this.store.get("local_runtime_service", serviceId);` |
| `src/v01211-runtime.ts` | `108.0` | 156:124 | 0 | `get(args: JsonObject = {}): JsonObject { return { service: this.store.get("local_runtime_service", String(args.service_id ?? "craft-local")) }; }` |
| `src/v01211-runtime.ts` | `114.0` | 170:37 | 0 | `const id = String(args.bundle_id ?? `project_bundle_${randomUUID().replaceAll("-", "")}`);` |
| `src/v01211-runtime.ts` | `116.0` | 172:69 | 0 | `if (existing) { if (existing.bundle_digest !== digest(identity)) throw new Error("Project Bundle idempotency conflict"); return { bundle: existing, idempotent: true }; }` |
| `src/v01211-runtime.ts` | `136.0` | 191:243 | 0 | `const identity = { scope, project_id: args.project_id === undefined ? null : text(args.project_id, "project_id"), task_id: args.task_id === undefined ? null : text(args.task_id, "task_id"), outcome_id: args.outcome_id === undefined ? null : text(args.outcome_id, "outcome_id"), action: text(args.action, "action"), diff_digest: text(args.diff_digest, "diff_digest"), reason_digest: digest(text(args.reason, "reason")), accepted: args.accepted === true };` |
| `src/v01211-runtime.ts` | `138.0` | 193:71 | 0 | `if (existing) { if (existing.identity_digest !== digest(identity)) throw new Error("Feedback signal idempotency conflict"); return { signal: existing, idempotent: true }; }` |
| `src/v01211-runtime.ts` | `154.0` | 211:49 | 0 | `const evaluatorId = String(args.evaluator_id ?? `domain_evaluator_${randomUUID().replaceAll("-", "")}`); const domain = text(args.domain, "domain"); const name = text(args.name, "name");` |
| `src/v01211-runtime.ts` | `168.0` | 231:45 | 0 | `const handoffId = String(args.handoff_id ?? `handoff_manifest_${randomUUID().replaceAll("-", "")}`); const identity = { task_id: text(args.task_id, "task_id"), session_id: args.session_id === undefined ? null : text(args.session_id, "session_id"), context_manifest_id: text(args.context_manifest_id, "context_manifest_id"), host: text(args.host, "host"), model: args.model === undefined ? null : text(args.model, "model"), allowed_effects: list(args.allowed_effects, "allowed_effects"), artifact_ids: list(args.artifact_ids, "artifact_ids"), evidence_ids: list(args.evidence_ids, "evidence_ids"), outcome_id: args.outcome_id === undefined ? null : text(args.outcome_id, "outcome_id") };` |
| `src/v01211-runtime.ts` | `184.0` | 247:36 | 0 | `const id = String(args.price_id ?? `price_${provider}_${model}`); const record = { provider, model, input_per_million: input, output_per_million: output, effective_at: args.effective_at === undefined ? new Date().toISOString() : text(args.effective_at, "effective_at") };` |
| `src/v01211-runtime.ts` | `186.0` | 247:231 | 0 | `const id = String(args.price_id ?? `price_${provider}_${model}`); const record = { provider, model, input_per_million: input, output_per_million: output, effective_at: args.effective_at === undefined ? new Date().toISOString() : text(args.effective_at, "effective_at") };` |
| `src/v01211-runtime.ts` | `187.0` | 248:85 | 0 | `const existing = this.store.find("provider_price", id); return { price: existing ? this.store.save("provider_price", id, { ...payload(existing), ...record }) : this.store.create("provider_price", id, record), idempotent: false };` |
| `src/v01211-runtime.ts` | `193.0` | 253:82 | 0 | `if (![input, output].every((value) => Number.isInteger(value) && value >= 0)) throw new Error("token counts must be non-negative integers");` |
| `src/v01211-runtime.ts` | `196.0` | 256:36 | 0 | `const id = String(args.usage_id ?? `usage_${randomUUID().replaceAll("-", "")}`); return { usage: this.store.create("usage_ledger", id, { provider, model, project_id: args.project_id ?? null, task_id: args.task_id ?? null, input_tokens: input, output_tokens: output, cost_usd: costUsd, price_id: price.id }), idempotent: false };` |
| `src/v01211-runtime.ts` | `208.0` | 259:351 | 0 | `report(args: JsonObject = {}): JsonObject { const projectId = args.project_id === undefined ? null : text(args.project_id, "project_id"); const entries = this.store.list("usage_ledger", 10_000, (item) => projectId === null \|\| item.project_id === projectId); return { entries, total_cost_usd: entries.reduce((sum, item) => sum + Number(item.cost_usd ?? 0), 0), total_input_tokens: entries.reduce((sum, item) => sum + Number(item.input_tokens ?? 0), 0), total_output_tokens: entries.reduce((sum, item) => sum + Number(item.output_tokens ?? 0), 0) }; }` |
| `src/v01211-runtime.ts` | `210.0` | 259:443 | 0 | `report(args: JsonObject = {}): JsonObject { const projectId = args.project_id === undefined ? null : text(args.project_id, "project_id"); const entries = this.store.list("usage_ledger", 10_000, (item) => projectId === null \|\| item.project_id === projectId); return { entries, total_cost_usd: entries.reduce((sum, item) => sum + Number(item.cost_usd ?? 0), 0), total_input_tokens: entries.reduce((sum, item) => sum + Number(item.input_tokens ?? 0), 0), total_output_tokens: entries.reduce((sum, item) => sum + Number(item.output_tokens ?? 0), 0) }; }` |
| `src/v01211-runtime.ts` | `212.0` | 259:537 | 0 | `report(args: JsonObject = {}): JsonObject { const projectId = args.project_id === undefined ? null : text(args.project_id, "project_id"); const entries = this.store.list("usage_ledger", 10_000, (item) => projectId === null \|\| item.project_id === projectId); return { entries, total_cost_usd: entries.reduce((sum, item) => sum + Number(item.cost_usd ?? 0), 0), total_input_tokens: entries.reduce((sum, item) => sum + Number(item.input_tokens ?? 0), 0), total_output_tokens: entries.reduce((sum, item) => sum + Number(item.output_tokens ?? 0), 0) }; }` |
| `src/v01212-verified-work.ts` | `2.0` | 5:50 | 0 | `if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`);` |
| `src/v01212-verified-work.ts` | `6.0` | 10:29 | 0 | `if (!Array.isArray(value)) throw new Error(`${name} must be an array`);` |
| `src/v01212-verified-work.ts` | `8.0` | 12:46 | 0 | `if (new Set(values).size !== values.length) throw new Error(`${name} must contain unique values`);` |
| `src/v01212-verified-work.ts` | `12.0` | 16:67 | 0 | `if (!value \|\| typeof value !== "object" \|\| Array.isArray(value)) throw new Error(`${name} must be an object`);` |
| `src/v01212-verified-work.ts` | `18.0` | 32:39 | 0 | `const workId = String(args.work_id ?? `verified_work_${randomUUID().replaceAll("-", "")}`);` |
| `src/v01212-verified-work.ts` | `24.0` | 34:39 | 0 | `if (!EFFECTS.has(identity.effect)) throw new Error("Unsupported verified work effect");` |
| `src/v01212-verified-work.ts` | `26.0` | 36:61 | 0 | `if (existing) { if (existing.work_digest !== workDigest) throw new Error("Verified Work idempotency conflict"); return { work: existing, idempotent: true }; }` |
| `src/v01212-verified-work.ts` | `29.0` | 42:79 | 0 | `if (!["prepared", "needs_replan", "paused"].includes(String(work.status))) throw new Error("Verified Work is not awaiting authorization");` |
| `src/v01212-verified-work.ts` | `41.0` | 52:67 | 0 | `if (work.status !== "authorized" && work.status !== "running") throw new Error("Verified Work is not authorized");` |
| `src/v01212-verified-work.ts` | `44.0` | 57:172 | 0 | `const saved = this.store.save("verified_work", String(work.id), { ...payload(work), status: "running", phase: "action_observed", action_count: Number(work.action_count ?? 0) + 1, last_receipt_id: receipt.id });` |
| `src/v01212-verified-work.ts` | `52.0` | 69:113 | 0 | `const work = this.store.get("verified_work", text(args.work_id, "work_id")); if (work.status !== "observed") throw new Error("Delivery requires a re-observed work state");` |
| `src/v01212-verified-work.ts` | `65.0` | 87:169 | 0 | `const work = this.store.get("verified_work", text(args.work_id, "work_id")); if (!["authorized", "running", "paused", "needs_replan"].includes(String(work.status))) throw new Error("Work is not handoffable");` |
| `src/v01212-verified-work.ts` | `73.0` | 103:38 | 0 | `const id = String(args.profile_id ?? `sandbox_conformance_${text(args.platform, "platform")}`); const platform = text(args.platform, "platform"); const checks = object(args.checks, "checks");` |
| `src/v01212-verified-work.ts` | `94.0` | 120:132 | 0 | `const limit = args.limit === undefined ? 100 : Number(args.limit); if (!Number.isInteger(limit) \|\| limit < 1 \|\| limit > 10_000) throw new Error("limit must be between 1 and 10000");` |
| `src/v01212-verified-work.ts` | `101.0` | 122:285 | 0 | `const rows = traces.map((trace) => ({ trace, events: this.store.list("trace_event", 10_000, (event) => event.trace_id === trace.id).map((event) => ({ id: event.id, sequence: event.sequence, event_kind: event.event_kind, source: event.source, trust: event.trust, usage: event.usage ?? null, data_digest: digest(event.data ?? {}) })) }));` |
| `src/v01212-verified-work.ts` | `102.0` | 122:325 | 0 | `const rows = traces.map((trace) => ({ trace, events: this.store.list("trace_event", 10_000, (event) => event.trace_id === trace.id).map((event) => ({ id: event.id, sequence: event.sequence, event_kind: event.event_kind, source: event.source, trust: event.trust, usage: event.usage ?? null, data_digest: digest(event.data ?? {}) })) }));` |
| `src/v01213-runtime.ts` | `6.0` | 12:67 | 0 | `if (!value \|\| typeof value !== "object" \|\| Array.isArray(value)) throw new Error(`${name} must be an object`);` |
| `src/v01213-runtime.ts` | `26.0` | 43:271 | 0 | `const contract = { task_id: text(args.task_id, "task_id"), workspace: text(args.workspace, "workspace"), operation: text(args.operation, "operation"), effect, input_digest: text(args.input_digest, "input_digest"), approval_ref: args.approval_ref === undefined ? null : text(args.approval_ref, "approval_ref") };` |
| `src/v01213-runtime.ts` | `34.0` | 56:38 | 0 | `if (action.status !== "prepared") throw new Error("Action is not executable");` |
| `src/v01213-runtime.ts` | `39.0` | 62:49 | 0 | `const relativePath = text(args.relative_path ?? "", "relative_path");` |
| `src/v01213-runtime.ts` | `40.0` | 64:6 | 0 | `? false` |
| `src/v01213-runtime.ts` | `42.0` | 66:48 | 0 | `const inDataRoot = isAbsolute(relativePath) && !relative(dataRoot, resolve(relativePath)).startsWith("..");` |
| `src/v01213-runtime.ts` | `46.0` | 68:66 | 0 | `const target = inWorkspace ? resolve(workspace, relativePath) : resolve(relativePath);` |
| `src/v01213-runtime.ts` | `51.0` | 75:36 | 0 | `if (effect !== "local_write") throw new Error("workspace_write requires local_write effect");` |
| `src/v01213-runtime.ts` | `68.0` | 109:113 | 0 | `const verdict = text(args.verdict, "verdict"); if (!(new Set(["passed", "failed", "blocked"]).has(verdict))) throw new Error("Unsupported acceptance verdict");` |
| `src/v01213-runtime.ts` | `85.0` | 133:43 | 0 | `const workerId = String(args.worker_id ?? "craft-worker"); const existing = this.store.find("durable_worker", workerId);` |
| `src/v01213-runtime.ts` | `93.0` | 138:116 | 0 | `start(args: JsonObject = {}): JsonObject { const worker = this.store.find("durable_worker", String(args.worker_id ?? "craft-worker")) ?? this.configure(args).worker as JsonObject; if (worker.status === "running") return { worker, idempotent: true }; return { worker: this.store.save("durable_worker", String(worker.id), { ...payload(worker), status: "running", started_at: new Date().toISOString() }), idempotent: false }; }` |
| `src/v01213-runtime.ts` | `94.0` | 138:136 | 0 | `start(args: JsonObject = {}): JsonObject { const worker = this.store.find("durable_worker", String(args.worker_id ?? "craft-worker")) ?? this.configure(args).worker as JsonObject; if (worker.status === "running") return { worker, idempotent: true }; return { worker: this.store.save("durable_worker", String(worker.id), { ...payload(worker), status: "running", started_at: new Date().toISOString() }), idempotent: false }; }` |
| `src/v01213-runtime.ts` | `98.0` | 139:114 | 0 | `stop(args: JsonObject = {}): JsonObject { const worker = this.store.get("durable_worker", String(args.worker_id ?? "craft-worker")); if (worker.status === "stopped") return { worker, idempotent: true }; return { worker: this.store.save("durable_worker", String(worker.id), { ...payload(worker), status: "stopped", stopped_at: new Date().toISOString() }), idempotent: false }; }` |
| `src/v01213-runtime.ts` | `102.0` | 141:81 | 0 | `enqueue(args: JsonObject): JsonObject { const workerId = String(args.worker_id ?? "craft-worker"); const jobId = String(args.job_id ?? `job_${randomUUID().replaceAll("-", "")}`); const existing = this.store.find("worker_job", jobId); if (existing) return { job: existing, idempotent: true }; return { job: this.store.create("worker_job", jobId, { worker_id: workerId, task_id: text(args.task_id, "task_id"), action: text(args.action, "action"), status: "pending", payload_digest: digest(args.payload ?? {}) }), idempotent: false }; }` |
| `src/v01213-runtime.ts` | `108.0` | 142:114 | 0 | `tick(args: JsonObject = {}): JsonObject { const worker = this.store.get("durable_worker", String(args.worker_id ?? "craft-worker")); if (worker.status !== "running") return { worker, jobs: [], skipped: true }; const now = String(args.now ?? new Date().toISOString()); const jobs = this.store.list("worker_job", 100, (item) => item.worker_id === worker.id && item.status === "pending").map((job) => this.store.save("worker_job", String(job.id), { ...payload(job), status: "leased", lease_id: `lease_${job.id}`, leased_at: now })); const saved = this.store.save("durable_worker", String(worker.id), { ...payload(worker), last_tick_at: now, processed_count: Number(worker.processed_count ?? 0) + jobs.length }); return { worker: saved, jobs, skipped: false }; }` |
| `src/v01213-runtime.ts` | `115.0` | 143:117 | 0 | `recover(args: JsonObject = {}): JsonObject { const worker = this.store.get("durable_worker", String(args.worker_id ?? "craft-worker")); const now = Date.parse(String(args.now ?? new Date().toISOString())); const recovered = this.store.list("worker_job", 100, (item) => item.worker_id === worker.id && item.status === "leased" && Date.parse(String(item.leased_at)) + Number(worker.lease_ttl_ms) < now).map((job) => this.store.save("worker_job", String(job.id), { ...payload(job), status: "pending", lease_id: null, recovered_at: new Date(now).toISOString() })); return { recovered, count: recovered.length }; }` |
| `src/v01213-runtime.ts` | `122.0` | 144:115 | 0 | `get(args: JsonObject = {}): JsonObject { return { worker: this.store.get("durable_worker", String(args.worker_id ?? "craft-worker")), jobs: this.store.list("worker_job", 100, (item) => item.worker_id === String(args.worker_id ?? "craft-worker")) }; }` |
| `src/v01213-runtime.ts` | `124.0` | 144:228 | 0 | `get(args: JsonObject = {}): JsonObject { return { worker: this.store.get("durable_worker", String(args.worker_id ?? "craft-worker")), jobs: this.store.list("worker_job", 100, (item) => item.worker_id === String(args.worker_id ?? "craft-worker")) }; }` |
| `src/v01213-runtime.ts` | `134.0` | 154:41 | 0 | `const routeId = String(args.route_id ?? `provider_route_${randomUUID().replaceAll("-", "")}`); const identity = { providers, preferred, fallback: providers.filter((item) => item !== preferred), task_id: args.task_id ?? null, budget_digest: digest(args.budget ?? {}) };` |
| `src/v01213-runtime.ts` | `144.0` | 158:406 | 0 | `record(args: JsonObject): JsonObject { const route = this.store.get("provider_route", text(args.route_id, "route_id")); const provider = text(args.provider, "provider"); if (!(route.providers as string[]).includes(provider)) throw new Error("Provider is not in the route"); const saved = this.store.save("provider_route", String(route.id), { ...payload(route), status: args.status === undefined ? "used" : text(args.status, "status"), selected_provider: provider, usage: args.usage === undefined ? null : object(args.usage, "usage") }); return { route: saved, idempotent: false }; }` |
| `src/v01213-runtime.ts` | `157.0` | 167:749 | 0 | `private async request(args: JsonObject, operation: string, body: JsonObject, fetchImpl: typeof fetch): Promise<JsonObject> { const endpoint = text(args.endpoint, "endpoint"); if (!endpoint.startsWith("https://")) throw new Error("A2A endpoint must use HTTPS"); const response = await fetchImpl(endpoint, { method: "POST", headers: { "content-type": "application/json", accept: "application/json" }, body: JSON.stringify({ jsonrpc: "2.0", method: operation, params: body, id: text(args.request_id ?? randomUUID(), "request_id") }) }); if (!response.ok) throw new Error(`A2A ${operation} failed: HTTP ${response.status}`); const value = await response.json() as JsonObject; return { operation, response_digest: digest(value), task_id: value.task_id ?? null, status: value.status ?? "accepted", raw_content: false }; }` |
| `src/v01213-runtime.ts` | `158.0` | 167:779 | 0 | `private async request(args: JsonObject, operation: string, body: JsonObject, fetchImpl: typeof fetch): Promise<JsonObject> { const endpoint = text(args.endpoint, "endpoint"); if (!endpoint.startsWith("https://")) throw new Error("A2A endpoint must use HTTPS"); const response = await fetchImpl(endpoint, { method: "POST", headers: { "content-type": "application/json", accept: "application/json" }, body: JSON.stringify({ jsonrpc: "2.0", method: operation, params: body, id: text(args.request_id ?? randomUUID(), "request_id") }) }); if (!response.ok) throw new Error(`A2A ${operation} failed: HTTP ${response.status}`); const value = await response.json() as JsonObject; return { operation, response_digest: digest(value), task_id: value.task_id ?? null, status: value.status ?? "accepted", raw_content: false }; }` |
| `src/v01226-runtime.ts` | `8.0` | 52:96 | 0 | `function list(value: unknown, name: string, minimum = 0): string[] { if (!Array.isArray(value)) throw new Error(`${name} must be an array`); const result = value.map((item) => required(item, name)); if (result.length < minimum \|\| new Set(result).size !== result.length) throw new Error(`${name} must contain unique values`); return result; }` |
| `src/v01226-runtime.ts` | `59.0` | 87:46 | 0 | `const version = Number(existing.version ?? 0) + 1;` |
| `src/v01226-runtime.ts` | `66.0` | 99:71 | 0 | `const supported = (manifest.platforms as string[]).includes("any") \|\| (manifest.platforms as string[]).includes(platform);` |
| `src/v01226-runtime.ts` | `74.0` | 117:42 | 0 | `if (current.status !== "quarantined") throw new Error("adapter must be quarantined before rollback");` |
| `src/v01226-runtime.ts` | `79.0` | 127:27 | 0 | `if (manifest.integrity && manifest.integrity !== actual) throw new Error("adapter integrity mismatch");` |
| `src/v01226-runtime.ts` | `80.0` | 127:61 | 0 | `if (manifest.integrity && manifest.integrity !== actual) throw new Error("adapter integrity mismatch");` |
| `src/v01226-runtime.ts` | `82.0` | 128:28 | 0 | `if (!manifest.signature && !expectedIntegrity) throw new Error("adapter install requires a signature or expected integrity");` |
| `src/v01226-runtime.ts` | `83.0` | 128:51 | 0 | `if (!manifest.signature && !expectedIntegrity) throw new Error("adapter install requires a signature or expected integrity");` |
| `src/v01226-runtime.ts` | `91.0` | 144:143 | 0 | `const child = this.spawnProcess(command, args, { cwd: request.cwd, env: { ...process.env, ...request.env }, shell: request.shell === false ? false : request.shell ?? false, windowsHide: true });` |
| `src/v01226-runtime.ts` | `93.0` | 146:39 | 0 | `const limit = request.output_limit ?? 64 * 1024;` |
| `src/v01226-runtime.ts` | `97.0` | 155:41 | 0 | `const timeout = request.timeout_ms ?? 120_000;` |
| `src/v01226-runtime.ts` | `116.0` | 162:166 | 0 | `const id = required(runId, "run_id"); const process = this.activeProcesses.get(id); if (!process) { const saved = this.store.find("command_run", id); if (!saved) throw new Error(`Unknown command_run: ${id}`); if (saved.status === "running") return { run: this.store.save("command_run", id, { ...saved, cancel_requested: true, cancel_requested_at: now() }), requested: false, cross_process: true }; return { run: saved, idempotent: true }; }` |
| `src/v01226-runtime.ts` | `117.0` | 162:245 | 0 | `const id = required(runId, "run_id"); const process = this.activeProcesses.get(id); if (!process) { const saved = this.store.find("command_run", id); if (!saved) throw new Error(`Unknown command_run: ${id}`); if (saved.status === "running") return { run: this.store.save("command_run", id, { ...saved, cancel_requested: true, cancel_requested_at: now() }), requested: false, cross_process: true }; return { run: saved, idempotent: true }; }` |
| `src/v01226-runtime.ts` | `122.0` | 167:116 | 0 | `const argv = list(input.argv, "argv", 1); const effect = input.effect ?? "read_only"; if (!EFFECTS.has(effect)) throw new Error("effect is unsupported");` |
| `src/v01226-runtime.ts` | `133.0` | 170:232 | 0 | `return { ...input, argv, effect, timeout_ms: integer(input.timeout_ms, "timeout_ms", 120_000, 100, 3_600_000), output_limit: integer(input.output_limit, "output_limit", 65_536, 256, 10_000_000), ...(input.cwd === undefined ? {} : { cwd: required(input.cwd, "cwd") }) };` |
| `src/v01226-runtime.ts` | `148.0` | 181:161 | 0 | `for (const candidate of input.candidates) { const cost = Number(candidate.token_cost ?? 100); const capability = String(candidate.capability ?? candidate.id ?? ""); if (requiredCapabilities.length && !requiredCapabilities.includes(capability)) { excluded.push({ ...candidate, reason: "not_required" }); continue; } if (spent + cost > budget) { excluded.push({ ...candidate, reason: "token_budget" }); continue; } selected.push(candidate); spent += cost; }` |
| `src/v01226-runtime.ts` | `152.0` | 181:346 | 0 | `for (const candidate of input.candidates) { const cost = Number(candidate.token_cost ?? 100); const capability = String(candidate.capability ?? candidate.id ?? ""); if (requiredCapabilities.length && !requiredCapabilities.includes(capability)) { excluded.push({ ...candidate, reason: "not_required" }); continue; } if (spent + cost > budget) { excluded.push({ ...candidate, reason: "token_budget" }); continue; } selected.push(candidate); spent += cost; }` |
| `src/v01226-runtime.ts` | `155.0` | 185:81 | 0 | `durableStart(input: JsonObject): JsonObject { const id = required(input.run_id ?? `durable_${randomUUID().replaceAll("-", "")}`, "run_id"); const existing = this.store.find("durable_run", id); if (existing) return { run: existing, idempotent: true }; return { run: this.store.create("durable_run", id, { ...input, run_id: id, status: "queued", attempts: 0, lease_until: null, created_at: now() }), idempotent: false }; }` |
| `src/v01226-runtime.ts` | `163.0` | 187:143 | 0 | `const queued = this.store.list("durable_run", 1, (record) => ["queued", "running"].includes(String(record.status)) && (!record.lease_until \|\| String(record.lease_until) < now())); const run = queued[0]; if (!run) return { claimed: false }; const claimed = this.store.save("durable_run", String(run.id), { ...run, status: "running", owner, attempts: Number(run.attempts ?? 0) + 1, lease_until: new Date(Date.now() + leaseSeconds * 1000).toISOString(), heartbeat_at: now() }); return { claimed: true, run: claimed };` |
| `src/v01226-runtime.ts` | `159.0` | 187:217 | 0 | `const queued = this.store.list("durable_run", 1, (record) => ["queued", "running"].includes(String(record.status)) && (!record.lease_until \|\| String(record.lease_until) < now())); const run = queued[0]; if (!run) return { claimed: false }; const claimed = this.store.save("durable_run", String(run.id), { ...run, status: "running", owner, attempts: Number(run.attempts ?? 0) + 1, lease_until: new Date(Date.now() + leaseSeconds * 1000).toISOString(), heartbeat_at: now() }); return { claimed: true, run: claimed };` |
| `src/v01226-runtime.ts` | `160.0` | 187:373 | 0 | `const queued = this.store.list("durable_run", 1, (record) => ["queued", "running"].includes(String(record.status)) && (!record.lease_until \|\| String(record.lease_until) < now())); const run = queued[0]; if (!run) return { claimed: false }; const claimed = this.store.save("durable_run", String(run.id), { ...run, status: "running", owner, attempts: Number(run.attempts ?? 0) + 1, lease_until: new Date(Date.now() + leaseSeconds * 1000).toISOString(), heartbeat_at: now() }); return { claimed: true, run: claimed };` |
| `src/v01226-runtime.ts` | `169.0` | 190:147 | 0 | `durableRecover(owner?: string): JsonObject { const runs = this.store.list("durable_run", 500, (record) => record.status === "running" && (!owner \|\| record.owner === owner)); const recovered = runs.map((run) => this.store.save("durable_run", String(run.id), { ...run, status: "queued", lease_until: null, recovered_at: now() })); return { recovered: recovered.length, runs: recovered }; }` |
| `src/v01226-runtime.ts` | `189.0` | 193:383 | 0 | `modelRoute(input: { candidates: JsonObject[]; objective?: "quality" \| "cost" \| "latency"; budget?: number }): JsonObject { const objective = input.objective ?? "quality"; if (!input.candidates.length) throw new Error("candidates must not be empty"); const ranked = [...input.candidates].sort((a, b) => { const score = (item: JsonObject) => objective === "cost" ? -Number(item.cost ?? Number.MAX_SAFE_INTEGER) : objective === "latency" ? -Number(item.latency_ms ?? Number.MAX_SAFE_INTEGER) : Number(item.quality ?? 0); return score(b) - score(a); }); const selected = ranked.find((candidate) => input.budget === undefined \|\| Number(candidate.cost ?? 0) <= input.budget) ?? ranked[0]; return { selected, ranked, rationale: `objective=${objective}` }; }` |
| `src/v01226-runtime.ts` | `191.0` | 193:463 | 0 | `modelRoute(input: { candidates: JsonObject[]; objective?: "quality" \| "cost" \| "latency"; budget?: number }): JsonObject { const objective = input.objective ?? "quality"; if (!input.candidates.length) throw new Error("candidates must not be empty"); const ranked = [...input.candidates].sort((a, b) => { const score = (item: JsonObject) => objective === "cost" ? -Number(item.cost ?? Number.MAX_SAFE_INTEGER) : objective === "latency" ? -Number(item.latency_ms ?? Number.MAX_SAFE_INTEGER) : Number(item.quality ?? 0); return score(b) - score(a); }); const selected = ranked.find((candidate) => input.budget === undefined \|\| Number(candidate.cost ?? 0) <= input.budget) ?? ranked[0]; return { selected, ranked, rationale: `objective=${objective}` }; }` |
| `src/v01226-runtime.ts` | `192.0` | 193:491 | 0 | `modelRoute(input: { candidates: JsonObject[]; objective?: "quality" \| "cost" \| "latency"; budget?: number }): JsonObject { const objective = input.objective ?? "quality"; if (!input.candidates.length) throw new Error("candidates must not be empty"); const ranked = [...input.candidates].sort((a, b) => { const score = (item: JsonObject) => objective === "cost" ? -Number(item.cost ?? Number.MAX_SAFE_INTEGER) : objective === "latency" ? -Number(item.latency_ms ?? Number.MAX_SAFE_INTEGER) : Number(item.quality ?? 0); return score(b) - score(a); }); const selected = ranked.find((candidate) => input.budget === undefined \|\| Number(candidate.cost ?? 0) <= input.budget) ?? ranked[0]; return { selected, ranked, rationale: `objective=${objective}` }; }` |
| `src/v01226-runtime.ts` | `185.0` | 193:671 | 0 | `modelRoute(input: { candidates: JsonObject[]; objective?: "quality" \| "cost" \| "latency"; budget?: number }): JsonObject { const objective = input.objective ?? "quality"; if (!input.candidates.length) throw new Error("candidates must not be empty"); const ranked = [...input.candidates].sort((a, b) => { const score = (item: JsonObject) => objective === "cost" ? -Number(item.cost ?? Number.MAX_SAFE_INTEGER) : objective === "latency" ? -Number(item.latency_ms ?? Number.MAX_SAFE_INTEGER) : Number(item.quality ?? 0); return score(b) - score(a); }); const selected = ranked.find((candidate) => input.budget === undefined \|\| Number(candidate.cost ?? 0) <= input.budget) ?? ranked[0]; return { selected, ranked, rationale: `objective=${objective}` }; }` |
| `src/v01226-runtime.ts` | `214.0` | 204:35 | 0 | `const paths = obj(document.paths ?? {}, "paths"); const operations: JsonObject[] = [];` |
| `src/web-operation.ts` | `2.0` | 5:50 | 0 | `if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`);` |
| `src/web-operation.ts` | `6.0` | 10:73 | 0 | `if (!Number.isInteger(number) \|\| number < minimum \|\| number > maximum) throw new Error(`${name} must be an integer between ${minimum} and ${maximum}`);` |
| `src/web-operation.ts` | `13.0` | 16:29 | 0 | `if (!Array.isArray(value)) throw new Error(`${name} must be an array`);` |
| `src/web-operation.ts` | `15.0` | 18:46 | 0 | `if (new Set(result).size !== result.length) throw new Error(`${name} must contain unique values`);` |
| `src/web-operation.ts` | `37.0` | 48:40 | 0 | `const id = String(args.operation_id ?? `web_${randomUUID().replaceAll("-", "")}`);` |
| `src/web-operation.ts` | `40.0` | 53:75 | 0 | `bytes: raw.length, truncated, status_state: response.ok ? "observed" : "failed", raw_content_stored: false });` |
| `src/web-operation.ts` | `48.0` | 61:40 | 0 | `const id = String(args.operation_id ?? `browser_${randomUUID().replaceAll("-", "")}`);` |
| `src/web-operation.ts` | `50.0` | 64:59 | 0 | `approval_ref: args.approval_ref === undefined ? null : text(args.approval_ref, "approval_ref"),` |
| `src/web-operation.ts` | `58.0` | 76:60 | 0 | `if (!["passed", "failed", "blocked"].includes(verdict)) throw new Error("unsupported browser operation verdict");` |
| `src/work-session.ts` | `2.0` | 5:102 | 0 | `function text(value: unknown, name: string): string { if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`); return value.trim(); }` |
| `src/work-session.ts` | `6.0` | 6:122 | 0 | `function strings(value: unknown, name: string): string[] { if (value === undefined) return []; if (!Array.isArray(value)) throw new Error(`${name} must be an array`); const result = value.map((item) => text(item, name)); if (new Set(result).size !== result.length) throw new Error(`${name} must contain unique values`); return result; }` |
| `src/work-session.ts` | `16.0` | 11:29 | 0 | `if (!Array.isArray(value)) throw new Error(`${name} must be an array`);` |
| `src/work-session.ts` | `31.0` | 22:67 | 0 | `if (task.project_id !== null && task.project_id !== projectId) throw new Error("Task does not belong to the Project Brain");` |
| `src/work-session.ts` | `48.0` | 36:15 | 0 | `if (!task) issues.push({ component: "task", issue: "missing" }); else if (Number(task.version) !== Number(session.task_version)) issues.push({ component: "task", issue: "version_changed", current_version: task.version });` |
| `src/work-session.ts` | `50.0` | 37:16 | 0 | `if (!brain) issues.push({ component: "project_brain", issue: "missing" }); else if (Number(brain.version) !== Number(session.brain_version ?? 1) && session.status !== "prepared") issues.push({ component: "project_brain", issue: "changed", current_version: brain.version });` |
| `src/work-session.ts` | `51.0` | 37:143 | 0 | `if (!brain) issues.push({ component: "project_brain", issue: "missing" }); else if (Number(brain.version) !== Number(session.brain_version ?? 1) && session.status !== "prepared") issues.push({ component: "project_brain", issue: "changed", current_version: brain.version });` |
| `src/work-session.ts` | `58.0` | 38:140 | 0 | `const status = issues.length ? "needs_replan" : session.status; const next = issues.length ? "review_context_drift" : session.launch_id ? "observe_execution" : "bind_work_launch";` |
| `src/work-session.ts` | `68.0` | 50:42 | 0 | `if (dispatch.session_id !== undefined && dispatch.session_id !== session.id) throw new Error("Internal dispatch is bound to another session");` |
| `src/work-session.ts` | `69.0` | 50:81 | 0 | `if (dispatch.session_id !== undefined && dispatch.session_id !== session.id) throw new Error("Internal dispatch is bound to another session");` |
| `src/workbench-experience.ts` | `2.0` | 4:102 | 0 | `function text(value: unknown, name: string): string { if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`); return value.trim(); }` |
| `src/workbench-experience.ts` | `5.0` | 6:77 | 0 | `function limit(value: unknown): number { const n = value === undefined ? 100 : Number(value); if (!Number.isInteger(n) \|\| n < 1 \|\| n > 1000) throw new Error("limit must be an integer between 1 and 1000"); return n; }` |
| `src/workbench-experience.ts` | `6.0` | 6:141 | 0 | `function limit(value: unknown): number { const n = value === undefined ? 100 : Number(value); if (!Number.isInteger(n) \|\| n < 1 \|\| n > 1000) throw new Error("limit must be an integer between 1 and 1000"); return n; }` |
| `src/workbench-experience.ts` | `24.0` | 15:145 | 0 | `const sessions = this.store.list("work_session", 10_000, (item) => (projectId === null \|\| item.project_id === projectId) && (taskId === null \|\| item.task_id === taskId) && (sessionId === null \|\| item.id === sessionId)).slice(0, n);` |
| `src/workbench-experience.ts` | `20.0` | 22:214 | 0 | `const nextAction = pilots.some((item) => item.status === "needs_replan") \|\| sessions.some((item) => item.status === "needs_replan") ? "review_context_drift" : sessions.some((item) => item.status === "running") ? "observe_execution" : launches.some((item) => item.status === "awaiting_approval") ? "approve_or_deny" : "start_or_prepare_work";` |
| `src/workbench-experience.ts` | `21.0` | 22:299 | 0 | `const nextAction = pilots.some((item) => item.status === "needs_replan") \|\| sessions.some((item) => item.status === "needs_replan") ? "review_context_drift" : sessions.some((item) => item.status === "running") ? "observe_execution" : launches.some((item) => item.status === "awaiting_approval") ? "approve_or_deny" : "start_or_prepare_work";` |
| `src/workbench-server.ts` | `58.0` | 104:93 | 0 | `return json(200, this.service.homeView(Number.isInteger(homeLimit) && homeLimit >= 1 && homeLimit <= 50 ? { limit: homeLimit } : {}));` |
| `src/workbench-server.ts` | `59.0` | 104:112 | 0 | `return json(200, this.service.homeView(Number.isInteger(homeLimit) && homeLimit >= 1 && homeLimit <= 50 ? { limit: homeLimit } : {}));` |
| `src/workbench-server.ts` | `62.0` | 106:69 | 0 | `if (request.method === "GET" && path === "/api/project-brain") return json(200, this.service.projectBrainGet({ project_id: url.searchParams.get("project_id") ?? "local", limit: url.searchParams.get("limit") ?? undefined }));` |
| `src/workbench-server.ts` | `65.0` | 107:76 | 0 | `if (request.method === "GET" && path === "/api/workbench-experience") return json(200, this.service.workbenchExperienceQuery({ project_id: url.searchParams.get("project_id") ?? undefined, task_id: url.searchParams.get("task_id") ?? undefined, limit: url.searchParams.get("limit") ?? undefined }));` |
| `src/workbench-server.ts` | `68.0` | 108:62 | 0 | `if (request.method === "GET" && path === "/api/traces") return json(200, this.service.traceQuery({ task_id: url.searchParams.get("task_id") ?? undefined, event_kind: url.searchParams.get("event_kind") ?? undefined, limit: url.searchParams.get("limit") ?? undefined }));` |
| `src/workbench-server.ts` | `71.0` | 109:77 | 0 | `if (request.method === "GET" && path === "/api/long-task-checkpoints") return json(200, this.service.longTaskList({ session_id: url.searchParams.get("session_id") ?? undefined, limit: url.searchParams.get("limit") ?? undefined }));` |
| `src/workbench-server.ts` | `113.0` | 122:158 | 0 | `if (request.method === "GET" && path === "/api/studio/resources") return json(200, this.service.studioResourceView({ kind: url.searchParams.get("kind") ?? undefined, task_id: url.searchParams.get("task_id") ?? undefined, limit: url.searchParams.get("limit") ?? undefined }));` |
| `src/workbench-server.ts` | `121.0` | 124:77 | 0 | `if (request.method === "POST" && path === "/api/studio/memory/review") return json(200, this.service.studioMemoryReview(bodyObject(request.body)));` |
| `src/workbench-server.ts` | `276.0` | 169:72 | 0 | `if (request.method === "GET" && path === "/api/studio/resources") return json(200, this.service.studioResourceView({ kind: url.searchParams.get("kind") ?? undefined, task_id: url.searchParams.get("task_id") ?? undefined, limit: url.searchParams.get("limit") ?? undefined }));` |
| `src/workbench-server.ts` | `285.0` | 172:70 | 0 | `if (request.method === "POST" && path === "/api/studio/memory") return json(201, this.service.studioMemorySave(bodyObject(request.body)));` |
| `src/workbench-server.ts` | `288.0` | 173:78 | 0 | `if (request.method === "POST" && path.startsWith("/api/studio/memory/") && path.endsWith("/retire")) return json(200, this.service.studioMemoryRetire({ memory_id: decodeURIComponent(path.slice(19, -7)) }));` |
| `src/workbench-server.ts` | `289.0` | 173:107 | 0 | `if (request.method === "POST" && path.startsWith("/api/studio/memory/") && path.endsWith("/retire")) return json(200, this.service.studioMemoryRetire({ memory_id: decodeURIComponent(path.slice(19, -7)) }));` |
| `src/workbench-server.ts` | `292.0` | 174:80 | 0 | `if (request.method === "POST" && path === "/api/studio/knowledge/claims") return json(201, this.service.studioKnowledgeClaimSave(bodyObject(request.body)));` |
| `src/workbench-server.ts` | `295.0` | 175:73 | 0 | `if (request.method === "POST" && path === "/api/studio/workflows") return json(201, this.service.studioWorkflowSave(bodyObject(request.body)));` |
| `src/workbench-server.ts` | `397.0` | 221:77 | 0 | `const body = bodyObject(request.body); const name = String(body.tool ?? "");` |
| `src/workbench.ts` | `168.0` | 205:53 | 0 | `if (SECRET.test(content) \|\| SECRET.test(source)) throw new Error("Memory content and source must not contain credentials or secrets");` |
| `src/workflow-dag.ts` | `29.0` | 30:118 | 0 | `const sideEffect = text(n.side_effect ?? "read_only", `nodes[${i}].side_effect`); if (!EFFECTS.has(sideEffect)) throw new Error(`unsupported side_effect: ${sideEffect}`);` |
| `src/workflow-dag.ts` | `59.0` | 45:190 | 0 | `const workflowId = String(args.workflow_id ?? id("workflow_dag")); const graph = this.validate(args); const lifecycle = String(args.lifecycle ?? "draft"); if (!LIFECYCLE.has(lifecycle)) throw new Error("workflow lifecycle is unsupported");` |
| `src/workflow-dag.ts` | `80.0` | 66:177 | 0 | `const runId = text(args.run_id, "run_id"); const workflow = this.store.get("workflow_dag", text(args.workflow_id, "workflow_id")); const state = { completed: args.completed ?? [], pending: args.pending ?? [], active: args.active ?? [], state_digest: text(args.state_digest ?? digest({ completed: args.completed ?? [], pending: args.pending ?? [], active: args.active ?? [] }), "state_digest") };` |
| `src/workflow-dag.ts` | `81.0` | 66:206 | 0 | `const runId = text(args.run_id, "run_id"); const workflow = this.store.get("workflow_dag", text(args.workflow_id, "workflow_id")); const state = { completed: args.completed ?? [], pending: args.pending ?? [], active: args.active ?? [], state_digest: text(args.state_digest ?? digest({ completed: args.completed ?? [], pending: args.pending ?? [], active: args.active ?? [] }), "state_digest") };` |
| `src/workflow-dag.ts` | `84.0` | 66:315 | 0 | `const runId = text(args.run_id, "run_id"); const workflow = this.store.get("workflow_dag", text(args.workflow_id, "workflow_id")); const state = { completed: args.completed ?? [], pending: args.pending ?? [], active: args.active ?? [], state_digest: text(args.state_digest ?? digest({ completed: args.completed ?? [], pending: args.pending ?? [], active: args.active ?? [] }), "state_digest") };` |
| `src/workflow-dag.ts` | `85.0` | 66:344 | 0 | `const runId = text(args.run_id, "run_id"); const workflow = this.store.get("workflow_dag", text(args.workflow_id, "workflow_id")); const state = { completed: args.completed ?? [], pending: args.pending ?? [], active: args.active ?? [], state_digest: text(args.state_digest ?? digest({ completed: args.completed ?? [], pending: args.pending ?? [], active: args.active ?? [] }), "state_digest") };` |
| `src/workflow-dag.ts` | `86.0` | 66:371 | 0 | `const runId = text(args.run_id, "run_id"); const workflow = this.store.get("workflow_dag", text(args.workflow_id, "workflow_id")); const state = { completed: args.completed ?? [], pending: args.pending ?? [], active: args.active ?? [], state_digest: text(args.state_digest ?? digest({ completed: args.completed ?? [], pending: args.pending ?? [], active: args.active ?? [] }), "state_digest") };` |

