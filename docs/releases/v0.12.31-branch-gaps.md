# Branch coverage gap report

- Generated from: `/Users/didi/Documents/work/craft/coverage/coverage-final.json`
- Source scope: runtime `src/**/*.ts` (CLI and declaration-only `.d.ts` files are not runtime logic)
- Covered branches: 18833/19043 (98.90%)
- Missing branches: 210

| File | Branch | Source location | Hits | Expression |
| --- | --- | ---: | ---: | --- |
| `src/a2a-v1-adapter.ts` | `40.0` | 44:269 | 0 | `const response = await fetchImpl(String(card.card_url), { method: "POST", headers: { "content-type": "application/json", accept: "application/json", "idempotency-key": requestId }, body: JSON.stringify(body) }); if (response.status < 200 \|\| response.status >= 300) throw new Error(`A2A v1 message/send HTTP ${response.status}`);` |
| `src/a2a-v1-adapter.ts` | `52.0` | 56:121 | 0 | `if (!new Set(["submitted", "working", "input-required", "completed", "failed", "canceled", "rejected"]).has(status)) throw new Error("A2A v1 task state is unsupported");` |
| `src/application/craft-service.ts` | `146.0` | 260:330 | 0 | `const nodes = Array.isArray(args.nodes) ? args.nodes : steps.map((step, index) => ({ id: String(step.id ?? `step-${index + 1}`), type: String(step.type ?? "action"), side_effect: String(step.side_effect ?? "read_only"), ...(step.action === undefined ? {} : { action: step.action }), depends_on: Array.isArray(step.depends_on) ? step.depends_on : [] }));` |
| `src/application/craft-service.ts` | `160.0` | 270:115 | 0 | `if (decision === "reviewed") { const ids = Array.isArray(claim.evidence_ids) ? claim.evidence_ids as unknown[] : []; if (!ids.some((e) => ["bounded", "confirmed"].includes(String(this.store.get("evidence", String(e)).confidence)))) throw new Error("Conflict resolution requires bounded or confirmed Evidence"); }` |
| `src/application/craft-service.ts` | `174.0` | 294:334 | 0 | `const prepared = this.actionGatewayPrepare({ action_id: `internal-write-${valueDigest(input).slice(7, 23)}`, task_id: text(input.task_id, "task_id"), workspace: text(input.workspace, "workspace"), operation: "workspace_write", effect: "local_write", input_digest: valueDigest(input), ...(input.approval_ref === undefined ? {} : { approval_ref: input.approval_ref }) });` |
| `src/application/craft-service.ts` | `676.0` | 1061:97 | 0 | `const unsafe = (workflow.steps as JsonObject[] ?? []).find((step) => String(step.side_effect ?? "read_only") !== "read_only");` |
| `src/application/craft-service.ts` | `970.0` | 1438:54 | 0 | ``Permission mode: ${String(task.permission_mode ?? "human_approval")}.`,` |
| `src/application/craft-service.ts` | `972.0` | 1454:140 | 0 | `this.store.appendEvent(`task:${taskId}`, "task.message.failed", { model_id: modelId, error_class: error instanceof Error ? error.name : "UnknownError" });` |
| `src/application/craft-service.ts` | `1372.0` | 2034:230 | 0 | `studioWorkflowCompatSave(args: JsonObject): JsonObject { return this.workflowSave({ workflow_id: args.workflow_id, name: text(args.name, "name"), description: args.description ?? "", inputs: args.inputs ?? [], steps: args.steps ?? [] }); }` |
| `src/application/craft-service.ts` | `2000.0` | 2315:94 | 0 | `const evidenceIds = Array.isArray(claim.evidence_ids) ? claim.evidence_ids as unknown[] : [];` |
| `src/application/craft-service.ts` | `2326.0` | 2590:140 | 0 | `const workspaceId = args.workspace_id === undefined ? `workspace_loop_${valueDigest({ root, include_paths: includePaths }).slice(-16)}` : text(args.workspace_id, "workspace_id"); const existing = this.store.find("workspace", workspaceId);` |
| `src/application/craft-service.ts` | `2328.0` | 2591:123 | 0 | `const workspace = existing ?? (this.workspaceOpen({ workspace_id: workspaceId, name: args.workspace_name ?? args.title ?? "Craft Task Workspace", root_path: root, include_paths: includePaths }).workspace as JsonObject);` |
| `src/application/craft-service.ts` | `2329.0` | 2592:108 | 0 | `if (workspace.root_path !== root \|\| valueDigest(workspace.include_paths) !== valueDigest(includePaths)) throw new Error("Task Workspace id is already bound to another root or observation scope");` |
| `src/application/craft-service.ts` | `2353.0` | 2630:98 | 0 | `const prompt = text(args.prompt, "prompt"); if (valueDigest(prompt) !== launch.prompt_digest) throw new Error("Execution Fabric prompt does not match the prepared digest");` |
| `src/application/craft-service.ts` | `2536.0` | 2865:13 | 0 | `if (!id) throw new Error("model id is required");` |
| `src/application/use-cases/adapter-runtime.ts` | `5.0` | 10:41 | 0 | `return value === undefined ? undefined : Number(value);` |
| `src/application/use-cases/adapter-runtime.ts` | `7.0` | 14:41 | 0 | `return value === undefined ? undefined : String(value);` |
| `src/application/use-cases/adapter-runtime.ts` | `29.0` | 76:157 | 0 | `serviceClass.prototype.durableRunRecover = function (args = {}) { return new V01226Runtime(this.store).durableRecover(args.owner === undefined ? undefined : String(args.owner)); };` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `27.0` | 36:94 | 0 | `const candidate = prepared.candidate as JsonObject; const reviewer = String(args.reviewer ?? "").trim(); const reason = String(args.reason ?? "").trim(); if (!reviewer \|\| !reason) throw new Error("reviewer and reason must not be empty");` |
| `src/application/use-cases/legacy-knowledge-migration.ts` | `28.0` | 36:143 | 0 | `const candidate = prepared.candidate as JsonObject; const reviewer = String(args.reviewer ?? "").trim(); const reason = String(args.reason ?? "").trim(); if (!reviewer \|\| !reason) throw new Error("reviewer and reason must not be empty");` |
| `src/capability-connector.ts` | `21.0` | 27:86 | 0 | `if (!result.length \|\| result.length > 20 \|\| new Set(result).size !== result.length) throw new Error("allowed_operations must contain between 1 and 20 unique entries");` |
| `src/capability-connector.ts` | `77.0` | 112:64 | 0 | `if (!["healthy", "degraded", "unhealthy"].includes(status)) throw new Error("Connector health status is unsupported");` |
| `src/capability-connector.ts` | `85.0` | 118:88 | 0 | `if (args.health_id !== undefined && text(args.health_id, "health_id") !== healthId) throw new Error("Connector health id is derived from connector_id");` |
| `src/capability-connector.ts` | `87.0` | 126:6 | 0 | `: this.store.create("capability_connector_health", healthId, healthPayload);` |
| `src/capability-connector.ts` | `136.0` | 206:86 | 0 | `connector_scope_digest: connector.scope_digest, connector_health_id: health?.id ?? null, connector_health_version: health?.version ?? null,` |
| `src/capability-connector.ts` | `138.0` | 206:137 | 0 | `connector_scope_digest: connector.scope_digest, connector_health_id: health?.id ?? null, connector_health_version: health?.version ?? null,` |
| `src/capability-connector.ts` | `146.0` | 218:66 | 0 | `if (connector.scope_digest !== ticket.connector_scope_digest) throw new Error("Connector scope changed after ticket issue");` |
| `src/home.ts` | `126.0` | 82:98 | 0 | `const knowledge = this.store.list("knowledge_claim", limit, (item) => item.task_id === taskId \|\| item.scope === `task:${taskId}`);` |
| `src/host-run.ts` | `43.0` | 26:1392 | 0 | `const completion = driver.execute({ ...args, dispatch_id: dispatchId, prompt }, { signal: controller.signal, observe }).then((result) => { const current = this.store.get("host_run", runId); const receipt = result.receipt as JsonObject; const status = current.cancel_requested \|\| receipt.cancelled ? "cancelled" : receipt.status === "completed" ? "completed" : "failed"; const saved = this.store.save("host_run", runId, { ...payload(current), status, receipt_id: receipt.id, finished_at: new Date().toISOString() }); this.store.appendEvent(`host-run:${runId}`, "host.finished", { status, receipt_id: receipt.id }); if (this.trace) { this.trace.append({ trace_id: traceId, event_kind: "host.finished", actor: "host-run", source: "host-run", trust: "observed", data: {}, output_refs: [String(receipt.id)], status, summary: "Host execution finished" }); this.trace.finalize({ trace_id: traceId, status: status === "completed" ? "completed" : status === "cancelled" ? "cancelled" : "failed", summary: "Host run terminal" }); } this.notifyTerminal(saved, receipt); }).catch((error) => { const current = this.store.get("host_run", runId); const saved = this.store.save("host_run", runId, { ...payload(current), status: current.cancel_requested ? "cancelled" : "failed", error_class: error instanceof Error ? error.name : "UnknownError", finished_at: new Date().toISOString() }); if (this.trace) { this.trace.append({ trace_id: traceId, event_kind: "host.failed", actor: "host-run", source: "host-run", trust: "observed", data: {}, error_class: error instanceof Error ? error.name : "UnknownError", status: "failed", summary: "Host execution failed" }); this.trace.finalize({ trace_id: traceId, status: "failed", summary: "Host execution failed" }); } this.notifyTerminal(saved, null); }).finally(() => { this.controllers.delete(runId); });` |
| `src/intent-compiler.ts` | `119.0` | 123:82 | 0 | `id: "coverage", name: `${String(intent.scope) === "changed" ? "Incremental" : "Workspace"} ${String(metric)} coverage`, method: "program", evaluator: "coverage_report",` |
| `src/intent-compiler.ts` | `124.0` | 134:71 | 0 | `if (existing.definition_digest !== definition.definition_digest) throw new Error("Acceptance contract idempotency conflict");` |
| `src/internal-host-driver.ts` | `11.0` | 60:102 | 0 | `const raw = typeof message.content === "string" ? message.content : message.content === null ? "" : JSON.stringify(message.content);` |
| `src/internal-host-driver.ts` | `48.0` | 123:55 | 0 | `session_id: args.session_id === undefined ? null : text(args.session_id, "session_id"),` |
| `src/internal-host-driver.ts` | `50.0` | 124:63 | 0 | `context_digest: args.context_digest === undefined ? null : text(args.context_digest, "context_digest"),` |
| `src/internal-host-driver.ts` | `52.0` | 125:63 | 0 | `acceptance_ref: args.acceptance_ref === undefined ? null : text(args.acceptance_ref, "acceptance_ref") };` |
| `src/internal-host-driver.ts` | `128.0` | 159:181 | 0 | `? (session.messages as JsonObject[]).map((item) => ({ role: String(item.role) as ConversationMessage["role"], content: typeof item.content === "string" ? redact(item.content) : null }))` |
| `src/internal-host-driver.ts` | `131.0` | 173:178 | 0 | `const parsedCalls = (result.tool_calls ?? []).map((call) => ({ id: call.id, name: call.function.name, arguments: (() => { try { return JSON.parse(call.function.arguments \|\| "{}"); } catch { throw new Error("tool arguments must be valid JSON"); } })() }));` |
| `src/internal-host-driver.ts` | `132.0` | 173:190 | 0 | `const parsedCalls = (result.tool_calls ?? []).map((call) => ({ id: call.id, name: call.function.name, arguments: (() => { try { return JSON.parse(call.function.arguments \|\| "{}"); } catch { throw new Error("tool arguments must be valid JSON"); } })() }));` |
| `src/internal-host-driver.ts` | `91.0` | 177:37 | 0 | `finalMessage = result.text \|\| (parsedCalls.length ? `Tool call ${parsedCalls[0]!.name} not executed` : "");` |
| `src/legacy-knowledge-migration.ts` | `6.0` | 19:102 | 0 | `function text(value: unknown, name: string): string { if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`); return value.trim(); }` |
| `src/legacy-knowledge-migration.ts` | `47.0` | 55:53 | 0 | `if (relative(sourceRoot, root).startsWith("..")) throw new Error("data/pages escapes source_root");` |
| `src/legacy-knowledge-migration.ts` | `76.0` | 85:8 | 0 | `} catch (error) { failures.push(this.failure(String(migration.id), "import", relPath, error instanceof Error ? error.message : "candidate_import_failed")); }` |
| `src/legacy-knowledge-migration.ts` | `109.0` | 122:159 | 0 | `if (item.isDirectory()) await walk(target); else if (item.isFile() && item.name.endsWith(".md")) { result.push(target); if (result.length > MAX_FILES) throw new Error("legacy knowledge file limit exceeded"); }` |
| `src/legacy-knowledge-migration.ts` | `117.0` | 132:82 | 0 | `const category = metadata.category ?? relative(pagesRoot, file).split("/")[0] ?? ""; const knowledge_type = metadata.knowledge_type ?? "";` |
| `src/legacy-knowledge-migration.ts` | `130.0` | 136:72 | 0 | `const summary = `来源摘要：${reuse}`; if (!safe(`${title}\n${summary}`)) return this.excluded(rel_path, page_digest, "sensitive_or_disallowed", title, category, knowledge_type);` |
| `src/legacy-knowledge-migration.ts` | `138.0` | 144:100 | 0 | `const resolved = await realpath(target); if (relative(sourceRoot, resolved).startsWith("..")) return "source_path_escape";` |
| `src/legacy-knowledge-migration.ts` | `140.0` | 145:86 | 0 | `const info = await stat(resolved); if (!info.isFile() \|\| info.size > MAX_BYTES) return "source_unavailable";` |
| `src/legacy-knowledge-migration.ts` | `142.0` | 146:76 | 0 | `const content = await readFile(resolved, "utf8"); if (!safe(content)) return "source_sensitive_or_disallowed";` |
| `src/long-task-worker.ts` | `10.0` | 7:161 | 0 | `function now(value: unknown): string { const result = value === undefined ? new Date().toISOString() : text(value, "now"); if (Number.isNaN(Date.parse(result))) throw new Error("now must be an ISO timestamp"); return result; }` |
| `src/long-task-worker.ts` | `19.0` | 16:149 | 0 | `if (taskRun && taskRun.contract_id && taskRun.launch_id && this.store.get("work_launch", String(taskRun.launch_id)).task_id !== session.task_id) throw new Error("Task Run does not belong to the session");` |
| `src/long-task-worker.ts` | `40.0` | 26:259 | 0 | `const signal = text(args.signal, "signal"); const saved = this.store.save("long_task_checkpoint", String(checkpoint.id), { ...payload(checkpoint), status: "wake_requested", wake_signal_digest: digest(signal), wake_reason: args.reason === undefined ? null : text(args.reason, "reason"), woken_at: now(args.now) }); return { checkpoint: saved, idempotent: false };` |
| `src/long-task-worker.ts` | `42.0` | 30:178 | 0 | `const checkpoint = this.store.get("long_task_checkpoint", text(args.checkpoint_id, "checkpoint_id")); if (!["wake_requested", "waiting"].includes(String(checkpoint.status))) throw new Error("Long Task checkpoint cannot resume from its current state");` |
| `src/long-task-worker.ts` | `45.0` | 31:264 | 0 | `const session = this.store.find("work_session", String(checkpoint.session_id)); const issues: JsonObject[] = []; if (!session) issues.push({ component: "work_session", issue: "missing" }); else if (Number(session.version) < Number(checkpoint.session_version)) issues.push({ component: "work_session", issue: "version_regressed" }); else if (session.context_digest !== checkpoint.context_digest) issues.push({ component: "work_session", issue: "context_changed" });` |
| `src/mcp-http.ts` | `37.0` | 55:102 | 0 | `await new Promise<void>((resolve, reject) => { http.once("error", reject); http.listen(options.port ?? 8787, options.host ?? "127.0.0.1", () => { http.removeListener("error", reject); resolve(); }); });` |
| `src/mcp-registry.ts` | `37.0` | 27:122 | 0 | `const source = this.store.get("mcp_registry_source", text(args.source_id, "source_id")); if (source.enabled !== true) throw new Error("MCP Registry source is disabled");` |
| `src/memory-governance.ts` | `53.0` | 41:212 | 0 | `for (const conflict of conflicts) this.store.save("memory_candidate", String(conflict.id), { ...payload(conflict), status: "conflict_pending", conflict_ids: [...new Set([...(conflict.conflict_ids as string[] ?? []), candidate.id])] });` |
| `src/memory-governance.ts` | `89.0` | 71:247 | 0 | `for (const conflictId of conflictIds) { const other = this.store.find("memory_candidate", String(conflictId)); if (other) this.store.save("memory_candidate", String(other.id), { ...payload(other), conflict_ids: (other.conflict_ids as string[] ?? []).filter((x) => x !== candidate.id), status: "candidate" }); }` |
| `src/memory-governance.ts` | `93.0` | 76:48 | 0 | `const now = new Date(args.now === undefined ? Date.now() : text(args.now, "now")); if (Number.isNaN(now.valueOf())) throw new Error("now must be an ISO timestamp"); const expired: JsonObject[] = [];` |
| `src/memory-governance.ts` | `114.0` | 89:57 | 0 | `const consolidationId = String(args.consolidation_id ?? id("memory_consolidation")); const identity = { candidate_ids: ids, summary_digest: digest(noSecret(text(args.summary, "summary"), "summary")) }; const existing = this.store.find("memory_consolidation", consolidationId);` |
| `src/model-gateway.ts` | `85.0` | 169:73 | 0 | `if (!["system", "user", "assistant", "tool"].includes(message.role)) throw new Error(`chat message ${index} has an unsupported role`);` |
| `src/model-gateway.ts` | `94.0` | 179:108 | 0 | `const system = messages.filter((message) => message.role === "system").map((message) => message.content ?? "").join("\n\n");` |
| `src/model-gateway.ts` | `102.0` | 184:210 | 0 | `...(system ? { system } : {}), ...(options.tools?.length ? { tools: options.tools.map((tool) => ({ name: tool.function.name, description: tool.function.description, input_schema: tool.function.parameters ?? { type: "object" } })) } : {}), ...(options.stream ? { stream: true } : {}) };` |
| `src/model-gateway.ts` | `71.0` | 184:264 | 0 | `...(system ? { system } : {}), ...(options.tools?.length ? { tools: options.tools.map((tool) => ({ name: tool.function.name, description: tool.function.description, input_schema: tool.function.parameters ?? { type: "object" } })) } : {}), ...(options.stream ? { stream: true } : {}) };` |
| `src/model-gateway.ts` | `149.0` | 212:163 | 0 | `return { id: String(item.id ?? `tool_${index + 1}`), type: "function" as const, function: { name: String(fn.name), arguments: typeof fn.arguments === "string" ? fn.arguments : JSON.stringify(fn.arguments ?? {}) } };` |
| `src/model-gateway.ts` | `150.0` | 212:208 | 0 | `return { id: String(item.id ?? `tool_${index + 1}`), type: "function" as const, function: { name: String(fn.name), arguments: typeof fn.arguments === "string" ? fn.arguments : JSON.stringify(fn.arguments ?? {}) } };` |
| `src/oidc-jwks.ts` | `53.0` | 74:75 | 0 | `const parts = text(token, "token").split("."); if (parts.length !== 3) throw new Error("OIDC token must be a compact JWT");` |
| `src/oidc-jwks.ts` | `59.0` | 80:4 | 0 | `catch { throw new Error("OIDC token key is invalid"); }` |
| `src/org-sync.ts` | `20.0` | 11:266 | 0 | `apply(args: JsonObject): JsonObject { const manifest = this.store.get("org_sync_manifest", text(args.sync_id, "sync_id")); const baseDigest = text(args.base_digest, "base_digest"); const currentDigest = args.current_digest === undefined ? manifest.manifest_digest : text(args.current_digest, "current_digest"); if (currentDigest !== baseDigest) return { status: "conflict", conflict: { expected: baseDigest, actual: currentDigest }, idempotent: false }; const applied = this.store.save("org_sync_manifest", String(manifest.id), { ...manifest, status: "applied", applied_at: new Date().toISOString(), tombstone_digest: digest(args.tombstones ?? []) }); return { status: "applied", manifest: applied, idempotent: false }; }` |
| `src/os-security.ts` | `9.0` | 7:119 | 0 | `function list(value: unknown, name: string): string[] { if (value === undefined) return []; if (!Array.isArray(value)) throw new Error(`${name} must be an array`); return value.map((item) => text(item, name)); }` |
| `src/os-security.ts` | `19.0` | 19:90 | 0 | `const network = text(args.network ?? "denied", "network"); if (!NETWORK.has(network)) throw new Error("Unsupported network policy");` |
| `src/os-security.ts` | `22.0` | 20:108 | 0 | `const filesystem = text(args.filesystem ?? "read_only", "filesystem"); if (!FILESYSTEM.has(filesystem)) throw new Error("Unsupported filesystem policy");` |
| `src/os-security.ts` | `28.0` | 23:80 | 0 | `const boundaryDigest = digest(boundary); const planId = String(args.plan_id ?? `os_security_${randomUUID().replaceAll("-", "")}`); const existing = this.store.find("os_security_plan", planId);` |
| `src/os-security.ts` | `30.0` | 24:69 | 0 | `if (existing) { if (existing.boundary_digest !== boundaryDigest) throw new Error("OS security plan idempotency conflict"); return { plan: existing, idempotent: true }; }` |
| `src/project-brain.ts` | `32.0` | 44:16 | 0 | `if (!brain) throw new Error(`Unknown Project Brain: ${project}`);` |
| `src/project-brain.ts` | `59.0` | 62:65 | 0 | `const record = { project_id: project, title: text(args.title ?? previous?.title, "title"), status: String(args.status ?? previous?.status ?? "active"), constraint_digests: list(args.constraint_digests ?? previous?.constraint_digests, "constraint_digests"), metric: args.metric === undefined ? previous?.metric ?? null : text(args.metric, "metric"), content_digest: digest({ title: args.title ?? previous?.title, constraints: args.constraint_digests ?? previous?.constraint_digests ?? [], metric: args.metric ?? previous?.metric ?? null }) };` |
| `src/project-brain.ts` | `66.0` | 62:305 | 0 | `const record = { project_id: project, title: text(args.title ?? previous?.title, "title"), status: String(args.status ?? previous?.status ?? "active"), constraint_digests: list(args.constraint_digests ?? previous?.constraint_digests, "constraint_digests"), metric: args.metric === undefined ? previous?.metric ?? null : text(args.metric, "metric"), content_digest: digest({ title: args.title ?? previous?.title, constraints: args.constraint_digests ?? previous?.constraint_digests ?? [], metric: args.metric ?? previous?.metric ?? null }) };` |
| `src/project-brain.ts` | `68.0` | 62:396 | 0 | `const record = { project_id: project, title: text(args.title ?? previous?.title, "title"), status: String(args.status ?? previous?.status ?? "active"), constraint_digests: list(args.constraint_digests ?? previous?.constraint_digests, "constraint_digests"), metric: args.metric === undefined ? previous?.metric ?? null : text(args.metric, "metric"), content_digest: digest({ title: args.title ?? previous?.title, constraints: args.constraint_digests ?? previous?.constraint_digests ?? [], metric: args.metric ?? previous?.metric ?? null }) };` |
| `src/project-brain.ts` | `73.0` | 62:523 | 0 | `const record = { project_id: project, title: text(args.title ?? previous?.title, "title"), status: String(args.status ?? previous?.status ?? "active"), constraint_digests: list(args.constraint_digests ?? previous?.constraint_digests, "constraint_digests"), metric: args.metric === undefined ? previous?.metric ?? null : text(args.metric, "metric"), content_digest: digest({ title: args.title ?? previous?.title, constraints: args.constraint_digests ?? previous?.constraint_digests ?? [], metric: args.metric ?? previous?.metric ?? null }) };` |
| `src/project-brain.ts` | `83.0` | 75:90 | 0 | `const previous = this.store.find("project_material", id); return { material: previous ? this.store.save("project_material", id, { ...payload(previous), ...material }) : this.store.create("project_material", id, material), idempotent: false };` |
| `src/project-brain.ts` | `86.0` | 79:246 | 0 | `const project = projectId(args.project_id); this.require(project); const id = String(args.outcome_id ?? `project_outcome_${randomUUID().replaceAll("-", "")}`); const outcome = { project_id: project, task_id: args.task_id === undefined ? null : text(args.task_id, "task_id"), session_id: args.session_id === undefined ? null : text(args.session_id, "session_id"), verdict: text(args.verdict, "verdict"), summary_digest: digest(text(args.summary, "summary")), evidence_ids: list(args.evidence_ids, "evidence_ids"), artifact_ids: list(args.artifact_ids, "artifact_ids"), status: "recorded" };` |
| `src/project-knowledge.ts` | `8.0` | 12:47 | 0 | `if (relative(root, target).startsWith("..")) throw new Error("Project Knowledge path escapes the trusted project root");` |
| `src/project-knowledge.ts` | `29.0` | 29:33 | 0 | `if (stat.isSymbolicLink()) throw new Error("Project Knowledge does not follow symbolic links");` |
| `src/remote-runtime.ts` | `12.0` | 14:32 | 0 | `if (!Number.isFinite(result)) throw new Error(`${name} must be an ISO timestamp`);` |
| `src/remote-runtime.ts` | `33.0` | 83:53 | 0 | `if (existing.binding_digest !== bindingDigest) throw new Error("Remote task binding idempotency conflict");` |
| `src/runtime-acceptance.ts` | `12.0` | 10:204 | 0 | `const values = value.map((item) => text(item, name)); if (new Set(values).size !== values.length \|\| exact !== undefined && values.length !== exact) throw new Error(`${name} must contain exactly ${exact ?? "unique"} values`);` |
| `src/runtime-acceptance.ts` | `16.0` | 13:192 | 0 | `function integer(value: unknown, name: string, minimum: number, maximum: number): number { const result = Number(value); if (!Number.isInteger(result) \|\| result < minimum \|\| result > maximum) throw new Error(`${name} must be an integer between ${minimum} and ${maximum}`); return result; }` |
| `src/runtime-acceptance.ts` | `22.0` | 30:39 | 0 | `const planId = String(args.plan_id ?? `runtime_acceptance_${randomUUID().replaceAll("-", "")}`); const existing = this.store.find("runtime_acceptance_plan", planId); const planDigest = digest(identity);` |
| `src/runtime-acceptance.ts` | `24.0` | 31:125 | 0 | `if (existing) { if (existing.plan_digest !== planDigest) throw new Error("Runtime acceptance plan idempotency conflict"); return { plan: existing, idempotent: true }; }` |
| `src/runtime-acceptance.ts` | `31.0` | 38:101 | 0 | `const caseId = text(args.case_id, "case_id"); if (!(plan.case_ids as string[]).includes(caseId)) throw new Error("Runtime acceptance Case is not in the plan");` |
| `src/runtime-acceptance.ts` | `51.0` | 48:65 | 0 | `if (existing) { if (existing.record_digest !== recordDigest) throw new Error("Runtime acceptance record idempotency conflict"); return { record: existing, idempotent: true }; }` |
| `src/runtime-acceptance.ts` | `53.0` | 50:19 | 0 | `if (duplicate) throw new Error("Runtime acceptance slot is already recorded");` |
| `src/runtime-acceptance.ts` | `76.0` | 66:118 | 0 | `const status = !complete ? "inconclusive" : strictProof ? "eligible" : baselineWins >= candidateWins ? "rejected" : "inconclusive";` |
| `src/runtime-acceptance.ts` | `79.0` | 69:143 | 0 | `if (existing) { if (existing.evaluation_digest !== evaluationDigest) throw new Error("Runtime acceptance evaluation idempotency conflict"); return { evaluation: existing, idempotent: true }; }` |
| `src/runtime-acceptance.ts` | `92.0` | 75:362 | 0 | `get(args: JsonObject): JsonObject { const plan = this.store.get("runtime_acceptance_plan", text(args.plan_id, "plan_id")); return { plan, records: this.store.list("runtime_acceptance_record", 10_000, (item) => item.plan_id === plan.id), evaluation: plan.latest_evaluation_id ? this.store.get("runtime_acceptance_evaluation", String(plan.latest_evaluation_id)) : null }; }` |
| `src/runtime-assurance.ts` | `22.0` | 42:54 | 0 | `const environmentDigest = digest(args.environment ?? {}); const budgetDigest = digest(args.budget ?? {});` |
| `src/runtime-assurance.ts` | `23.0` | 42:102 | 0 | `const environmentDigest = digest(args.environment ?? {}); const budgetDigest = digest(args.budget ?? {});` |
| `src/runtime-assurance.ts` | `28.0` | 48:66 | 0 | `if (!(contract.allowed_effects as string[]).includes(effect)) throw new Error("Runtime Assurance effect is not allowed by the Task Contract");` |
| `src/runtime-assurance.ts` | `50.0` | 67:75 | 0 | `if (existing) { if (existing.attestation_digest !== attestationDigest) throw new Error("Runtime Assurance attestation idempotency conflict"); return { attestation: existing, idempotent: true }; }` |
| `src/runtime-assurance.ts` | `56.0` | 76:102 | 0 | `const evidenceIds = ids(args.evidence_ids, "evidence_ids"); for (const evidenceId of evidenceIds) this.store.get("evidence", evidenceId);` |
| `src/runtime-assurance.ts` | `58.0` | 81:18 | 0 | `if (existing) { if (existing.intervention_digest !== interventionDigest) throw new Error("Runtime Assurance intervention idempotency conflict"); return { intervention: existing, idempotent: true }; }` |
| `src/runtime-assurance.ts` | `69.0` | 96:23 | 0 | `if (!matches[0]) throw new Error("Runtime Assurance Campaign requires a verified attestation for every bound Task Run");` |
| `src/runtime-assurance.ts` | `64.0` | 103:63 | 0 | `if (existing.assurance_campaign_digest !== recordDigest) throw new Error("Runtime Assurance Campaign idempotency conflict");` |
| `src/runtime-assurance.ts` | `65.0` | 108:63 | 0 | `status: (result.runner as JsonObject).campaign_lifecycle ?? "collecting" });` |
| `src/runtime-assurance.ts` | `83.0` | 133:50 | 0 | `if (workspace.root_path !== launch.workspace) throw new Error("Runtime Assurance Workspace observation is outside the Task Run workspace");` |
| `src/runtime-truth-kernel.ts` | `2.0` | 6:102 | 0 | `function text(value: unknown, name: string): string { if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`); return value.trim(); }` |
| `src/runtime-truth-kernel.ts` | `18.0` | 24:79 | 0 | `const events = args.events === undefined ? [] : Array.isArray(args.events) ? args.events as JsonObject[] : (() => { throw new Error("events must be an array"); })();` |
| `src/runtime-truth-kernel.ts` | `24.0` | 34:35 | 0 | `const id = text(args.export_id ?? `trace_otlp_${randomUUID().replaceAll("-", "")}`, "export_id");` |
| `src/runtime-truth-kernel.ts` | `31.0` | 44:115 | 0 | `const result = compactConversation(messages as ConversationMessage[], args.max_chars === undefined ? undefined : Number(args.max_chars));` |
| `src/runtime-truth.ts` | `59.0` | 100:33 | 0 | `try { parsed = new URL(url); } catch { throw new Error("endpoint must be a valid HTTP(S) URL"); }` |
| `src/runtime-truth.ts` | `83.0` | 129:74 | 0 | `const name = text(fn.name, "tool name"); const rawArgs = fn.arguments ?? {};` |
| `src/runtime-truth.ts` | `114.0` | 150:108 | 0 | `const tail: ConversationMessage[] = []; let used = system.reduce((sum, item) => sum + String(item.content ?? "").length, 0);` |
| `src/runtime-truth.ts` | `104.0` | 152:63 | 0 | `const item = messages[index]!; if (item.role === "system") continue;` |
| `src/runtime-truth.ts` | `105.0` | 153:35 | 0 | `if (used + String(item.content ?? "").length > Math.floor(maxChars * 0.65)) break;` |
| `src/runtime-truth.ts` | `108.0` | 154:52 | 0 | `tail.unshift(item); used += String(item.content ?? "").length;` |
| `src/service-foundation.ts` | `8.0` | 314:32 | 0 | `: configuredModels.length ? configuredModels : PROVIDER_CATALOG;` |
| `src/settings.ts` | `22.0` | 63:2 | 0 | `catch { throw new Error(`${name} must be a valid HTTP(S) URL`); }` |
| `src/settings.ts` | `33.0` | 73:66 | 0 | `if (!item \|\| typeof item !== "object" \|\| Array.isArray(item)) throw new Error(`models[${index}] must be an object`);` |
| `src/settings.ts` | `78.0` | 135:39 | 0 | `models: patch.models !== undefined ? patch.models : current.models,` |
| `src/state-workspace.ts` | `12.0` | 13:47 | 0 | `if (relative(root, target).startsWith("..")) throw new Error("workspace state path escapes root");` |
| `src/store.ts` | `9.0` | 25:29 | 0 | `return Number(row?.value ?? 0);` |
| `src/supervisor.ts` | `76.0` | 49:48 | 0 | `try { return { state: await this.start(port), owned: true }; }` |
| `src/supply-chain-attestation.ts` | `8.0` | 10:134 | 0 | `function sha256(value: unknown, name: string): string { const result = text(value, name); if (!/^sha256:[a-f0-9]{64}$/u.test(result)) throw new Error(`${name} must be a SHA-256 digest`); return result; }` |
| `src/supply-chain-attestation.ts` | `12.0` | 25:4 | 0 | `catch { throw new Error("publisher public_key_pem is invalid"); }` |
| `src/supply-chain-attestation.ts` | `14.0` | 28:18 | 0 | `if (existing) { if (existing.definition_digest !== definitionDigest) throw new Error("Supply-chain publisher idempotency conflict"); return { publisher: existing, idempotent: true }; }` |
| `src/supply-chain-attestation.ts` | `17.0` | 33:140 | 0 | `const publisher = this.store.get("supply_chain_publisher", text(args.publisher_id, "publisher_id")); if (publisher.status !== "active") throw new Error("Supply-chain publisher is not active");` |
| `src/supply-chain-attestation.ts` | `20.0` | 38:4 | 0 | `catch { throw new Error("Supply-chain signature is invalid"); }` |
| `src/supply-chain-attestation.ts` | `22.0` | 39:19 | 0 | `if (!verified) throw new Error("Supply-chain signature does not verify");` |
| `src/supply-chain-attestation.ts` | `24.0` | 41:53 | 0 | `const attestationId = String(args.attestation_id ?? `supply_attestation_${digest(identity).slice(-24)}`); const existing = this.store.find("supply_chain_attestation", attestationId); const attestationDigest = digest(identity);` |
| `src/supply-chain-attestation.ts` | `25.0` | 42:18 | 0 | `if (existing) { if (existing.attestation_digest !== attestationDigest) throw new Error("Supply-chain attestation idempotency conflict"); return { attestation: existing, idempotent: true }; }` |
| `src/supply-chain-attestation.ts` | `28.0` | 47:152 | 0 | `const attestation = this.store.get("supply_chain_attestation", text(args.attestation_id, "attestation_id")); if (attestation.status !== "verified") throw new Error("Supply-chain attestation is not active");` |
| `src/supply-chain-attestation.ts` | `33.0` | 54:151 | 0 | `const attestation = this.store.get("supply_chain_attestation", text(args.attestation_id, "attestation_id")); if (attestation.status === "revoked") return { attestation, idempotent: true };` |
| `src/task-state.ts` | `12.0` | 14:140 | 0 | `const current = this.store.find("task_state_projection", taskId); const expected = args.expected_revision === undefined ? Number(current?.state_revision ?? 0) : Number(args.expected_revision); if (!Number.isInteger(expected) \|\| expected < 0) throw new Error("expected_revision must be a non-negative integer");` |
| `src/trace-kernel.ts` | `140.0` | 144:177 | 0 | `const archived: JsonObject[] = this.store.list("trace_archive", Number.MAX_SAFE_INTEGER, (archive) => (traceId === null \|\| archive.trace_id === traceId) && (taskId === null \|\| archive.task_id === taskId)).flatMap((archive): JsonObject[] => {` |
| `src/trace-kernel.ts` | `143.0` | 146:64 | 0 | `return bundle.events.filter((event) => eventKind === null \|\| event.event_kind === eventKind).map((event) => ({ ...event, archived: true, archive_uri: archive.archive_uri }));` |
| `src/trace-kernel.ts` | `146.0` | 148:76 | 0 | `const events = [...hot, ...archived].sort((a, b) => String(a.created_at ?? "").localeCompare(String(b.created_at ?? ""))).slice(0, limit);` |
| `src/trace-kernel.ts` | `147.0` | 148:117 | 0 | `const events = [...hot, ...archived].sort((a, b) => String(a.created_at ?? "").localeCompare(String(b.created_at ?? ""))).slice(0, limit);` |
| `src/trace-kernel.ts` | `190.0` | 169:552 | 0 | `const existing = this.store.find("trace_policy", policyId); const identity = { max_days: maxDays, max_events: maxEvents, pii_mode: String(args.pii_mode ?? "digest_only") }; if (existing) { if (existing.identity_digest === digest(identity)) return { policy: existing, idempotent: true }; if (args.replace !== true) throw new Error("Trace policy idempotency conflict"); return { policy: this.store.save("trace_policy", policyId, { ...payload(existing), ...identity, identity_digest: digest(identity), policy_revision: Number(existing.policy_revision ?? 1) + 1, updated_by: String(args.updated_by ?? "operator") }), idempotent: false }; }` |
| `src/trace-kernel.ts` | `213.0` | 183:63 | 0 | `.filter((trace) => Date.parse(String(trace.last_event_at ?? trace.updated_at ?? trace.started_at)) < cutoff)` |
| `src/trace-kernel.ts` | `214.0` | 183:83 | 0 | `.filter((trace) => Date.parse(String(trace.last_event_at ?? trace.updated_at ?? trace.started_at)) < cutoff)` |
| `src/trace-kernel.ts` | `206.0` | 189:31 | 0 | `const archive = existing ? this.archivePointer(existing) : this.archives.write({ trace_id: String(trace.id), trace_version: Number(trace.version), archived_at: now, trace, events, feedback });` |
| `src/trace-kernel.ts` | `208.0` | 190:225 | 0 | `if (!existing) this.store.create("trace_archive", String(trace.id), { trace_id: trace.id, trace_version: trace.version, task_id: trace.task_id, status: trace.status, archived_at: now, last_event_at: trace.last_event_at ?? trace.updated_at, event_count: events.length, feedback_count: feedback.length, archive_backend_id: archive.backend_id ?? "local", archive_storage: archive.storage, archive_locator: archive.locator, archive_uri: archive.uri, archive_format: archive.format, content_digest: archive.content_digest, bytes: archive.bytes });` |
| `src/trace-kernel.ts` | `209.0` | 190:346 | 0 | `if (!existing) this.store.create("trace_archive", String(trace.id), { trace_id: trace.id, trace_version: trace.version, task_id: trace.task_id, status: trace.status, archived_at: now, last_event_at: trace.last_event_at ?? trace.updated_at, event_count: events.length, feedback_count: feedback.length, archive_backend_id: archive.backend_id ?? "local", archive_storage: archive.storage, archive_locator: archive.locator, archive_uri: archive.uri, archive_format: archive.format, content_digest: archive.content_digest, bytes: archive.bytes });` |
| `src/trace-kernel.ts` | `225.0` | 203:57 | 0 | `return { ...(record.archive_backend_id === undefined ? {} : { backend_id: text(record.archive_backend_id, "archive_backend_id") }), storage: String(record.archive_storage) as TraceArchivePointer["storage"], format: String(record.archive_format) as TraceArchivePointer["format"], locator: text(record.archive_locator, "archive_locator"), uri: text(record.archive_uri, "archive_uri"), content_digest: text(record.content_digest, "archive content_digest"), bytes: Number(record.bytes) };` |
| `src/trace-kernel.ts` | `230.0` | 209:69 | 0 | `if (stored) return { id, revision: Number(stored.policy_revision ?? 1), maxDays: Number(stored.max_days), source: "stored" };` |
| `src/trace-kernel.ts` | `232.0` | 210:26 | 0 | `if (id !== "default") throw new Error("Unknown Trace retention policy");` |
| `src/trust-profile.ts` | `14.0` | 19:27 | 0 | `if (value === undefined) return [];` |
| `src/trust-profile.ts` | `63.0` | 99:56 | 0 | `project_id: input.project_id === undefined ? null : text(input.project_id, "scope.project_id"),` |
| `src/v01211-runtime.ts` | `12.0` | 12:93 | 0 | `function numberOrZero(value: unknown): number { return value === undefined \|\| value === null ? 0 : Number(value); }` |
| `src/v01211-runtime.ts` | `18.0` | 18:44 | 0 | `if (new Set(items).size !== items.length) throw new Error(`${name} must contain unique values`);` |
| `src/v01211-runtime.ts` | `22.0` | 23:67 | 0 | `if (!value \|\| typeof value !== "object" \|\| Array.isArray(value)) throw new Error(`${name} must be an object`);` |
| `src/v01211-runtime.ts` | `52.0` | 93:24 | 0 | `if (!events.length) throw new Error("Trace has no replayable action contracts");` |
| `src/v01211-runtime.ts` | `64.0` | 108:38 | 0 | `if (replay.status !== "prepared") throw new Error("Replay is not prepared");` |
| `src/v01211-runtime.ts` | `86.0` | 139:140 | 0 | `const serviceId = String(fallback(args.service_id, "craft-local")); const service = this.store.find("local_runtime_service", serviceId) ?? (this.configure({ service_id: serviceId }).service as JsonObject);` |
| `src/v01211-runtime.ts` | `108.0` | 176:69 | 0 | `if (existing) { if (existing.bundle_digest !== digest(identity)) throw new Error("Project Bundle idempotency conflict"); return { bundle: existing, idempotent: true }; }` |
| `src/v01211-runtime.ts` | `124.0` | 197:71 | 0 | `if (existing) { if (existing.identity_digest !== digest(identity)) throw new Error("Feedback signal idempotency conflict"); return { signal: existing, idempotent: true }; }` |
| `src/v01211-runtime.ts` | `162.0` | 251:239 | 0 | `const id = String(fallback(args.price_id, `price_${provider}_${model}`)); const record = { provider, model, input_per_million: input, output_per_million: output, effective_at: args.effective_at === undefined ? new Date().toISOString() : text(args.effective_at, "effective_at") };` |
| `src/v01211-runtime.ts` | `163.0` | 252:85 | 0 | `const existing = this.store.find("provider_price", id); return { price: existing ? this.store.save("provider_price", id, { ...payload(existing), ...record }) : this.store.create("provider_price", id, record), idempotent: false };` |
| `src/v01211-runtime.ts` | `169.0` | 257:82 | 0 | `if (![input, output].every((value) => Number.isInteger(value) && value >= 0)) throw new Error("token counts must be non-negative integers");` |
| `src/v01212-verified-work.ts` | `8.0` | 10:29 | 0 | `if (!Array.isArray(value)) throw new Error(`${name} must be an array`);` |
| `src/v01212-verified-work.ts` | `10.0` | 12:46 | 0 | `if (new Set(values).size !== values.length) throw new Error(`${name} must contain unique values`);` |
| `src/v01212-verified-work.ts` | `27.0` | 34:39 | 0 | `if (!EFFECTS.has(identity.effect)) throw new Error("Unsupported verified work effect");` |
| `src/v01212-verified-work.ts` | `30.0` | 36:61 | 0 | `if (existing) { if (existing.work_digest !== workDigest) throw new Error("Verified Work idempotency conflict"); return { work: existing, idempotent: true }; }` |
| `src/v01212-verified-work.ts` | `46.0` | 52:67 | 0 | `if (work.status !== "authorized" && work.status !== "running") throw new Error("Verified Work is not authorized");` |
| `src/v01213-runtime.ts` | `37.0` | 58:38 | 0 | `if (action.status !== "prepared") throw new Error("Action is not executable");` |
| `src/v01213-runtime.ts` | `42.0` | 66:6 | 0 | `? false` |
| `src/v01213-runtime.ts` | `44.0` | 68:48 | 0 | `const inDataRoot = isAbsolute(relativePath) && !relative(dataRoot, resolve(relativePath)).startsWith("..");` |
| `src/v01213-runtime.ts` | `48.0` | 70:66 | 0 | `const target = inWorkspace ? resolve(workspace, relativePath) : resolve(relativePath);` |
| `src/v01213-runtime.ts` | `53.0` | 77:36 | 0 | `if (effect !== "local_write") throw new Error("workspace_write requires local_write effect");` |
| `src/v01213-runtime.ts` | `69.0` | 111:113 | 0 | `const verdict = text(args.verdict, "verdict"); if (!(new Set(["passed", "failed", "blocked"]).has(verdict))) throw new Error("Unsupported acceptance verdict");` |
| `src/v01226-runtime.ts` | `8.0` | 52:96 | 0 | `function list(value: unknown, name: string, minimum = 0): string[] { if (!Array.isArray(value)) throw new Error(`${name} must be an array`); const result = value.map((item) => required(item, name)); if (result.length < minimum \|\| new Set(result).size !== result.length) throw new Error(`${name} must contain unique values`); return result; }` |
| `src/v01226-runtime.ts` | `59.0` | 87:46 | 0 | `const version = Number(existing.version ?? 0) + 1;` |
| `src/v01226-runtime.ts` | `80.0` | 127:27 | 0 | `if (manifest.integrity && manifest.integrity !== actual) throw new Error("adapter integrity mismatch");` |
| `src/v01226-runtime.ts` | `81.0` | 127:61 | 0 | `if (manifest.integrity && manifest.integrity !== actual) throw new Error("adapter integrity mismatch");` |
| `src/v01226-runtime.ts` | `95.0` | 146:39 | 0 | `const limit = request.output_limit ?? 64 * 1024;` |
| `src/v01226-runtime.ts` | `99.0` | 155:41 | 0 | `const timeout = request.timeout_ms ?? 120_000;` |
| `src/v01226-runtime.ts` | `126.0` | 167:116 | 0 | `const argv = list(input.argv, "argv", 1); const effect = input.effect ?? "read_only"; if (!EFFECTS.has(effect)) throw new Error("effect is unsupported");` |
| `src/v01226-runtime.ts` | `152.0` | 181:161 | 0 | `for (const candidate of input.candidates) { const cost = Number(candidate.token_cost ?? 100); const capability = String(candidate.capability ?? candidate.id ?? ""); if (requiredCapabilities.length && !requiredCapabilities.includes(capability)) { excluded.push({ ...candidate, reason: "not_required" }); continue; } if (spent + cost > budget) { excluded.push({ ...candidate, reason: "token_budget" }); continue; } selected.push(candidate); spent += cost; }` |
| `src/v01226-runtime.ts` | `165.0` | 187:373 | 0 | `const queued = this.store.list("durable_run", 1, (record) => ["queued", "running"].includes(String(record.status)) && (!record.lease_until \|\| String(record.lease_until) < now())); const run = queued[0]; if (!run) return { claimed: false }; const claimed = this.store.save("durable_run", String(run.id), { ...run, status: "running", owner, attempts: Number(run.attempts ?? 0) + 1, lease_until: new Date(Date.now() + leaseSeconds * 1000).toISOString(), heartbeat_at: now() }); return { claimed: true, run: claimed };` |
| `src/v01226-runtime.ts` | `194.0` | 193:383 | 0 | `modelRoute(input: { candidates: JsonObject[]; objective?: "quality" \| "cost" \| "latency"; budget?: number }): JsonObject { const objective = input.objective ?? "quality"; if (!input.candidates.length) throw new Error("candidates must not be empty"); const ranked = [...input.candidates].sort((a, b) => { const score = (item: JsonObject) => objective === "cost" ? -Number(item.cost ?? Number.MAX_SAFE_INTEGER) : objective === "latency" ? -Number(item.latency_ms ?? Number.MAX_SAFE_INTEGER) : Number(item.quality ?? 0); return score(b) - score(a); }); const selected = ranked.find((candidate) => input.budget === undefined \|\| Number(candidate.cost ?? 0) <= input.budget) ?? ranked[0]; return { selected, ranked, rationale: `objective=${objective}` }; }` |
| `src/v01226-runtime.ts` | `196.0` | 193:463 | 0 | `modelRoute(input: { candidates: JsonObject[]; objective?: "quality" \| "cost" \| "latency"; budget?: number }): JsonObject { const objective = input.objective ?? "quality"; if (!input.candidates.length) throw new Error("candidates must not be empty"); const ranked = [...input.candidates].sort((a, b) => { const score = (item: JsonObject) => objective === "cost" ? -Number(item.cost ?? Number.MAX_SAFE_INTEGER) : objective === "latency" ? -Number(item.latency_ms ?? Number.MAX_SAFE_INTEGER) : Number(item.quality ?? 0); return score(b) - score(a); }); const selected = ranked.find((candidate) => input.budget === undefined \|\| Number(candidate.cost ?? 0) <= input.budget) ?? ranked[0]; return { selected, ranked, rationale: `objective=${objective}` }; }` |
| `src/v01226-runtime.ts` | `197.0` | 193:491 | 0 | `modelRoute(input: { candidates: JsonObject[]; objective?: "quality" \| "cost" \| "latency"; budget?: number }): JsonObject { const objective = input.objective ?? "quality"; if (!input.candidates.length) throw new Error("candidates must not be empty"); const ranked = [...input.candidates].sort((a, b) => { const score = (item: JsonObject) => objective === "cost" ? -Number(item.cost ?? Number.MAX_SAFE_INTEGER) : objective === "latency" ? -Number(item.latency_ms ?? Number.MAX_SAFE_INTEGER) : Number(item.quality ?? 0); return score(b) - score(a); }); const selected = ranked.find((candidate) => input.budget === undefined \|\| Number(candidate.cost ?? 0) <= input.budget) ?? ranked[0]; return { selected, ranked, rationale: `objective=${objective}` }; }` |
| `src/v01226-runtime.ts` | `219.0` | 204:35 | 0 | `const paths = obj(document.paths ?? {}, "paths"); const operations: JsonObject[] = [];` |
| `src/web-operation.ts` | `8.0` | 10:73 | 0 | `if (!Number.isInteger(number) \|\| number < minimum \|\| number > maximum) throw new Error(`${name} must be an integer between ${minimum} and ${maximum}`);` |
| `src/web-operation.ts` | `15.0` | 16:29 | 0 | `if (!Array.isArray(value)) throw new Error(`${name} must be an array`);` |
| `src/web-operation.ts` | `17.0` | 18:46 | 0 | `if (new Set(result).size !== result.length) throw new Error(`${name} must contain unique values`);` |
| `src/web-operation.ts` | `39.0` | 48:40 | 0 | `const id = String(args.operation_id ?? `web_${randomUUID().replaceAll("-", "")}`);` |
| `src/web-operation.ts` | `42.0` | 53:75 | 0 | `bytes: raw.length, truncated, status_state: response.ok ? "observed" : "failed", raw_content_stored: false });` |
| `src/web-operation.ts` | `50.0` | 61:40 | 0 | `const id = String(args.operation_id ?? `browser_${randomUUID().replaceAll("-", "")}`);` |
| `src/web-operation.ts` | `52.0` | 64:59 | 0 | `approval_ref: args.approval_ref === undefined ? null : text(args.approval_ref, "approval_ref"),` |
| `src/web-operation.ts` | `61.0` | 76:60 | 0 | `if (!["passed", "failed", "blocked"].includes(verdict)) throw new Error("unsupported browser operation verdict");` |
| `src/work-session.ts` | `8.0` | 6:122 | 0 | `function strings(value: unknown, name: string): string[] { if (value === undefined) return []; if (!Array.isArray(value)) throw new Error(`${name} must be an array`); const result = value.map((item) => text(item, name)); if (new Set(result).size !== result.length) throw new Error(`${name} must contain unique values`); return result; }` |
| `src/work-session.ts` | `18.0` | 11:29 | 0 | `if (!Array.isArray(value)) throw new Error(`${name} must be an array`);` |
| `src/work-session.ts` | `33.0` | 22:67 | 0 | `if (task.project_id !== null && task.project_id !== projectId) throw new Error("Task does not belong to the Project Brain");` |
| `src/work-session.ts` | `56.0` | 37:143 | 0 | `if (!brain) issues.push({ component: "project_brain", issue: "missing" }); else if (Number(brain.version) !== Number(session.brain_version ?? 1) && session.status !== "prepared") issues.push({ component: "project_brain", issue: "changed", current_version: brain.version });` |
| `src/work-session.ts` | `63.0` | 38:140 | 0 | `const status = issues.length ? "needs_replan" : session.status; const next = issues.length ? "review_context_drift" : session.launch_id ? "observe_execution" : "bind_work_launch";` |
| `src/work-session.ts` | `73.0` | 50:42 | 0 | `if (dispatch.session_id !== undefined && dispatch.session_id !== session.id) throw new Error("Internal dispatch is bound to another session");` |
| `src/work-session.ts` | `74.0` | 50:81 | 0 | `if (dispatch.session_id !== undefined && dispatch.session_id !== session.id) throw new Error("Internal dispatch is bound to another session");` |
| `src/workbench-experience.ts` | `2.0` | 4:102 | 0 | `function text(value: unknown, name: string): string { if (typeof value !== "string" \|\| !value.trim()) throw new Error(`${name} must not be empty`); return value.trim(); }` |
| `src/workbench-experience.ts` | `7.0` | 6:141 | 0 | `function limit(value: unknown): number { const n = value === undefined ? 100 : Number(value); if (!Number.isInteger(n) \|\| n < 1 \|\| n > 1000) throw new Error("limit must be an integer between 1 and 1000"); return n; }` |
| `src/workbench-experience.ts` | `25.0` | 15:145 | 0 | `const sessions = this.store.list("work_session", 10_000, (item) => (projectId === null \|\| item.project_id === projectId) && (taskId === null \|\| item.task_id === taskId) && (sessionId === null \|\| item.id === sessionId)).slice(0, n);` |
| `src/workbench-experience.ts` | `21.0` | 22:214 | 0 | `const nextAction = pilots.some((item) => item.status === "needs_replan") \|\| sessions.some((item) => item.status === "needs_replan") ? "review_context_drift" : sessions.some((item) => item.status === "running") ? "observe_execution" : launches.some((item) => item.status === "awaiting_approval") ? "approve_or_deny" : "start_or_prepare_work";` |
| `src/workbench-experience.ts` | `22.0` | 22:299 | 0 | `const nextAction = pilots.some((item) => item.status === "needs_replan") \|\| sessions.some((item) => item.status === "needs_replan") ? "review_context_drift" : sessions.some((item) => item.status === "running") ? "observe_execution" : launches.some((item) => item.status === "awaiting_approval") ? "approve_or_deny" : "start_or_prepare_work";` |
| `src/workbench-server.ts` | `279.0` | 170:72 | 0 | `if (request.method === "GET" && path === "/api/studio/resources") return json(200, this.service.studioResourceView({ kind: queryValue(url, "kind"), task_id: queryValue(url, "task_id"), limit: queryValue(url, "limit") }));` |
| `src/workbench-server.ts` | `288.0` | 173:70 | 0 | `if (request.method === "POST" && path === "/api/studio/memory") return json(201, this.service.studioMemorySave(bodyObject(request.body)));` |
| `src/workbench-server.ts` | `291.0` | 174:78 | 0 | `if (request.method === "POST" && path.startsWith("/api/studio/memory/") && path.endsWith("/retire")) return json(200, this.service.studioMemoryRetire({ memory_id: decodeURIComponent(path.slice(19, -7)) }));` |
| `src/workbench-server.ts` | `292.0` | 174:107 | 0 | `if (request.method === "POST" && path.startsWith("/api/studio/memory/") && path.endsWith("/retire")) return json(200, this.service.studioMemoryRetire({ memory_id: decodeURIComponent(path.slice(19, -7)) }));` |
| `src/workbench-server.ts` | `295.0` | 175:80 | 0 | `if (request.method === "POST" && path === "/api/studio/knowledge/claims") return json(201, this.service.studioKnowledgeClaimSave(bodyObject(request.body)));` |
| `src/workbench-server.ts` | `298.0` | 176:73 | 0 | `if (request.method === "POST" && path === "/api/studio/workflows") return json(201, this.service.studioWorkflowSave(bodyObject(request.body)));` |
| `src/workbench-server.ts` | `400.0` | 222:77 | 0 | `const body = bodyObject(request.body); const name = String(body.tool ?? "");` |
| `src/workflow-dag.ts` | `59.0` | 45:190 | 0 | `const workflowId = String(args.workflow_id ?? id("workflow_dag")); const graph = this.validate(args); const lifecycle = String(args.lifecycle ?? "draft"); if (!LIFECYCLE.has(lifecycle)) throw new Error("workflow lifecycle is unsupported");` |

