# Platform v1 同时要求机制证据与用户价值证据

Craft 的 Platform Ideal State v1 不能只由协议测试和覆盖率宣告完成。每次 Release Qualification 都必须先通过权限、恢复、再观察、验收和回滚的机制门，再用同输入、同模型、同环境和等预算的 Reference Pilot 与普通 Codex 基线配对比较；至少一个预先声明的主要指标改善且其他关键指标无越界退化，才能形成 `eligible`，证据不足必须标记 `inconclusive`。
