# Wiki 到能力候选：受控提炼

v0.11.31 可从至少两条 `reviewed` Knowledge Claim 创建 Skill 或 Workflow 候选。候选固定 Claim 的精确版本、适用条件、指令草案与回退条件。

它始终是 `draft` 或 `ready_for_evaluation`，没有执行权限，也不会自动生成 `SKILL.md`、发布 Workflow 或修改默认路由。后续必须走独立评测、审核与既有发布门禁。
