# Global Agent Rules

- 始终用简体中文。先说明假设；有歧义、上下文缺失、API 不清或依赖冲突时，列出问题并确认，不猜。

## Implementation Sufficiency

- 参考项目代码选型（java js等等），否则优先使用python写脚本处理逻辑
- 编码前先说明思路和可验证的验收条件、非目标；必须先查找当前项目 1～2 个最接近的实现，并检查其调用链、测试、配置和仓库规范。以当前项目代码为首要参考，优先沿用既有边界与惯例；发现明显缺陷或与需求冲突时说明差异。
- 采用最小充分实现。新增文件、依赖、抽象或配置必须服务当前验收条件、既有约束或已证实风险；优先复用项目代码、标准库和已有依赖。
- 完成时，每条验收条件都要有代码、测试、日志或成功运行作为证据；处理与改动相关的异常、校验、兼容性、安全、数据一致性、并发和可观测性。检查最终 diff，删除无当前用途的代码并报告未验证风险。

## Network

- 命令行使用 `proxy`、`unproxy` 切换外网与内网访问。

## Skills

- `~/.agents/skills` 是 Claude 和 Codex 的统一技能入口：`d-skills` 安装项作为内部实体放 `~/.SmartWork/skills` 并链接进 `.agents`，其他外部实体放 `.agents`；同名时外部原版优先于 SkillHub 镜像。

### Craft 子能力

- 每次任务开始和结束，优先复用可信 Craft Hook；Hook 不可用时，分别调用 `craft-knowledge`、`craft-memory`、`craft-experience`。
- `craft-codebase`：仅在需要 checkpoint 固定的符号、静态调用方或候选影响范围时调用；局部文件修改和运行时判断使用常规源码、测试与诊断工具。

## 测试策略

- 根据改动范围和风险自行选择充分且必要的测试，优先运行直接相关测试和受影响模块测试，一般不运行全量测试。
- 涉及公共基础能力或影响范围无法可靠判断时，可扩大测试范围。
- 完成时说明已运行的测试及未覆盖风险。
