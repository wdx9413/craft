[package]
name = "craft-workbench-desktop"
version = "0.1.0"
description = "Tauri desktop shell for the Craft Workbench"
authors = ["Craft"]
edition = "2021"

[lib]
name = "craft_workbench_desktop_lib"
crate-type = ["cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-global-shortcut = "2"
tauri-plugin-notification = "2"
serde = { version = "1", features = ["derive"] }
