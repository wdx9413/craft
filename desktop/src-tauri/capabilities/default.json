{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-workbench",
  "description": "Only the trusted local Workbench may invoke the small desktop command surface.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
