{
  "name": "craft",
  "version": "0.3.0",
  "description": "Capability discovery, durable tasks, immutable execution trials, evaluation signoff, and verified workflows.",
  "skills": "./skills/",
  "mcpServers": {
    "craft": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/plugin/craft-mcp.cjs"]
    }
  }
}
