{
  "name": "craft",
  "version": "0.4.0",
  "description": "Capability discovery, durable tasks, immutable execution trials, multi-source grading, signoff, and verified workflows.",
  "skills": "./skills/",
  "mcpServers": {
    "craft": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/plugin/craft-mcp.cjs"]
    }
  }
}
